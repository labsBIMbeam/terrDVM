## VERIFICATION PASSED

**Phase:** 1 — Sandboxed Bbox and Orthophoto UI · **Plans:** 9 · **Status:** 0 blockers; all 9 warnings closed; 6 non-blocking residuals.

### Closure of the nine warnings (evidence from the revised artifacts)

1. **Stale runner script name + siblings** — 01-02-02 step 10 now reads `node scripts/verify-source-policy.mjs` (matches the file 01-05-01 creates), so the Plan 05 wave no longer SKIPs source-policy. `"gate:phase-01": "node scripts/phase-01-gate.mjs --require-pass"` is wired with the flag and matches the 01-VALIDATION Machine-gate row. The 01-01-02/03 VALIDATION row now credits the auto task with producing `package-audit.md` and the checkpoint with verifying it. **Closed.**
2. **01-06-03 unsatisfiable verify** — now `const c=require('.../cases.json'); if(!c.cases.some(x=>x.id==='preview-ready-live'))`. Sibling one-liners audited: 01-02-02 (`m.cases` array check), 01-07-01, 01-08-01 all use `.cases`; 01-05-01 uses `p.roles`. No shell-level `;` chaining survives in any `<automated>` block (remaining semicolons are inside `node -e` strings and do not match 01-08-03's `;`-then-`test -s/-f` hygiene pattern). **Closed.**
3. **`pnpm lint` in Plan 02** — root script is `eslint --no-error-on-unmatched-pattern apps/napplet/src apps/napplet/tests scripts`, with the rationale pinned inline ("`apps/napplet/tests/` is not created until Task 2"). Task 1 verify and runner step 3 are now satisfiable on the pre-tests tree. **Closed.**
4. **`verify-no-local-surface` clauses (a)/(c)** — clause (a) now allowlists "the Vite preview launcher and deterministic harness-lifetime fault interposer, both path-scoped to `apps/napplet/tests/smoke/`", matching clause (c)'s harness-lifetime preview/interposer ports and 01-07-01's harness-process-owned interposer. No production/source/tool path is allowlisted. **Closed.**
5. **Basemap-only continuation, correctly fenced** — 01-05-02 adds `APPROVE-BASEMAP-ONLY` as an exact outcome with its own resume value; 01-05-03's precondition accepts it for MAP-01/02 only. Fencing verified end-to-end: the Plan 05 ladder still runs schema mode (satisfiable under a blocked imagery role), 01-06-01's precondition requires `--require-live` exit 0, and 01-06-03 is the only place the hard step lands — so a basemap-only outcome delivers draw/edit/clear and leaves Plans 06–09 honestly blocked, never MAP-05/06 completion. **Closed.**
6. **OPS-01 applied to the G2 blocker, timing coherent** — 01-05-01 appends and validates a ledger entry for any role blocked >30 min *before* the checkpoint ("no >30-minute blocked role may reach the checkpoint without one", restated in `<done>`); 01-05-02's BLOCKED branch requires it and explicitly routes blocked-imagery-with-live-basemap to APPROVE-BASEMAP-ONLY "and still requires the >30-minute OPS-01 ledger entry". Ledger + validator exist from Wave 1 (01-01-01); gate check 12 re-validates. **Closed.**
7. **16 requirement→evidence pairs pinned** — checked-in `scripts/phase-01-evidence-map.json` (14 owned IDs + VER-04-T1/T3, each with evidence path, producer task, machine predicate); MAP-03/MAP-04/MAP-07 get dedicated `.planning/evidence/phase-01/requirements/<ID>.json` records materialized in 01-08-02. Gate check 5 and the EVIDENCE-INDEX generator read only this map; 01-09-01's verify asserts 16 rows and all 16 IDs. Present in 01-08 `files_modified` and both task file lists. **Closed.**
8. **File-count boundary** — the fix was flagged optional; 02/03/05 remain at exactly 15 (not over) and 01-03-01 keeps 10 files. 01-08 rose to exactly 15 with the evidence map and `requirements/*` — still at, not past, the threshold. **Closed as accepted residual.**
9. **Validation flag ownership** — 01-VALIDATION now carries an explicit ownership line: the plan checker sets `nyquist_compliant`; execution task 01-09-03 sets `status: approved` and `wave_0_complete: true` after gate PASS + sign-off. 01-08-02 still freezes frontmatter, and 01-09-03 performs the flip, so the file's own sign-off checklist can complete. **Closed.**

### Allowed-output SHA binding (re-derived)
`allowed_outputs` now enumerates clean-checkout.log, `clean/*`, gate-verdict.json, EVIDENCE-INDEX.md, public-diff-review.md **and the Phase 1 VALIDATION status file** — walking the chain, 01-09-01's gate-evidence commit keeps check 4 true, and 01-09-03's sign-off block + public-diff-review + VALIDATION status edit all fall inside checks 3/4 and the sign-off diff rule. The binding survives its own commits.

### Residual notes (non-blocking)
- **R1** — Three stale enumerations predate the new fourth outcome: 01-05 `<verification>` ("approved / re-test / blocked"), 01-05 success criterion, and the 01-VALIDATION 01-05-01 row ("approves ONLY a live-verified policy"). The authoritative checkpoint block, resume-signal, must_haves truth 3, and 01-05-03's precondition are all correct; the drift errs fail-closed. Fix: add `approved-basemap-only` to those three enumerations.
- **R2** — 01-09-03 sets `nyquist_compliant: true` although the new ownership line assigns that flag to the plan checker (01-08-02's parenthetical assigns all three flags to the checker). Documentation-only: no gate check reads the flag. Fix: drop `nyquist_compliant` from 01-09-03's edit list.
- **R3** — `phase-01-gate.mjs`'s writer contract does not state that it preserves an existing `human_signoff` block, yet 01-09-03 runs a final `--require-pass` re-proof *after* the sign-off write (and its verify asserts the block before that re-run). Pre-existing accepted design; one clause in 01-08-01 ("the gate preserves any existing human_signoff block") removes the hazard.
- **R4** — Nothing in 01-08-01 asserts the checked-in evidence map itself holds exactly 16 keys; coverage comes from the fixture-based index test (01-08-03) and gate check 5 at Plan 09, so a typo surfaces late.
- **R5** — 01-05-01's `<files>` omits `.planning/evidence/fallback-ledger.jsonl` though its action conditionally appends to it; the plan-level `.planning/evidence/*` entry covers it.
- **R6** — 01-09-01's verify re-runs `verify-clean-checkout.sh`, leaving regenerated log/verdict uncommitted at plan end; these are allowed_outputs, so check 3 tolerates it, but sign-off's clean-tree precondition means the executor must make one more output-only commit.

Plans verified. Run `/gsd-execute-phase 1` to proceed.
