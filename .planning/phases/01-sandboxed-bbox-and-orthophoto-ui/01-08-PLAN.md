---
phase: 01-sandboxed-bbox-and-orthophoto-ui
plan: 08
type: execute
wave: 8
depends_on: ["01-07"]
files_modified:
  - apps/napplet/tests/smoke/ui-evidence-harness.mjs
  - apps/napplet/tests/smoke/a11y-contract.mjs
  - apps/napplet/tests/smoke/paja-harness.mjs
  - apps/napplet/tests/smoke/cases.json
  - scripts/verify-clean-checkout.sh
  - scripts/phase-01-gate.mjs
  - scripts/phase-01-signoff.mjs
  - scripts/phase-01-evidence-map.json
  - scripts/tests/gate-tooling.test.mjs
  - package.json
  - .planning/evidence/phase-01/ui/*
  - .planning/evidence/phase-01/a11y-contract.json
  - .planning/evidence/phase-01/runtime-measurements.json
  - .planning/evidence/phase-01/requirements/*
  - .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04, MAP-05, MAP-06, MAP-07, MAP-08, SBOX-01, SBOX-02, SBOX-03, SBOX-04, VER-02, OPS-01]
must_haves:
  truths:
    - "All 15 UI-SPEC evidence states are captured from the BUILT artifact under real Paja (never a dev server) at desktop and 320px, plus 2x DPR, reduced-motion, forced-colors, keyboard-only, and the long-attribution backstop — each capture's live/fixture/unavailable outcome is derived from the rendered DOM source-row suffix and indicator state and cross-checked against the two-role source-policy record, never authored by the harness"
    - "The a11y contract is asserted mechanically from the rendered DOM with dependency-free in-repo checks (contrast of rendered pairs, focus-visible ring, aria-live/role assignments, 44px targets, label associations, reduced-motion behavior), and a11y-contract.json records the checker's provenance explicitly — in-repo rendered-DOM checks, not axe-core — so the claim stays auditable"
    - "The complete Plan 09 gate tooling exists, is unit-tested, and is COMMITTED before any evidence generation: verify-clean-checkout.sh (fail-closed, SHA-bound, worktree at the exact captured HEAD, required-artifact assertions, frozen install, full manifest suite, unique preview port env), phase-01-gate.mjs (13 named checks, run_id/generated_at freshness, generated EVIDENCE-INDEX with 16 rows), and phase-01-signoff.mjs (refuses any FAIL/stale/unexpected-diff state)"
    - "The quick runner and the full runner are separate wired commands, and both runtimes are MEASURED in this plan from real runs (cold clean install excluded from both targets) and written into 01-VALIDATION.md as observed values — no asserted runtime claim survives unmeasured"
    - "All non-final evidence (UI captures, a11y results, refreshed smoke/negative evidence) is generated from the built artifact and request logs, redacted, and committed by the end of Task 2"
  artifacts:
    - path: "apps/napplet/tests/smoke/ui-evidence-harness.mjs"
      provides: "Built-artifact capture of the 15 UI-SPEC states with DOM-derived outcome flags and per-capture provenance"
      contains: "forced-colors"
    - path: "apps/napplet/tests/smoke/a11y-contract.mjs"
      provides: "Dependency-free rendered-DOM accessibility checks with recorded checker provenance"
      contains: "provenance"
    - path: "scripts/verify-clean-checkout.sh"
      provides: "Fail-closed SHA-bound clean-checkout runner: preflight-clean assertion, HEAD SHA as first log line, worktree at that exact SHA, committed-artifact assertions, frozen install, full manifest suite, TERRDVM_PREVIEW_PORT isolation, evidence copy-back"
      contains: "rev-parse HEAD"
    - path: "scripts/phase-01-gate.mjs"
      provides: "Machine gate: 13 named checks, gate-verdict.json with run_id/generated_at/tested_sha/allowed_outputs, EVIDENCE-INDEX.md generator (16 rows), --require-pass mode"
      contains: "--require-pass"
    - path: "scripts/phase-01-signoff.mjs"
      provides: "Sign-off recorder that recomputes the gate and refuses approved on FAIL, staleness, tested-SHA mismatch, dirty tree, or any diff beyond the committed gate outputs"
      contains: "tested_sha"
    - path: "scripts/tests/gate-tooling.test.mjs"
      provides: "Unit tests for gate schema, freshness rejection, check-count enforcement, and signoff refusal paths"
    - path: ".planning/evidence/phase-01/runtime-measurements.json"
      provides: "Observed quick and full runner runtimes from this plan's real runs"
      contains: "full_runner_seconds"
  key_links:
    - from: "apps/napplet/tests/smoke/cases.json"
      to: "scripts/verify-phase-01.sh"
      via: "ui-states and a11y-contract manifest entries appended so the runner executes them automatically"
      pattern: "ui-states"
    - from: "apps/napplet/tests/smoke/ui-evidence-harness.mjs"
      to: "apps/napplet/src/config/source-policy.json"
      via: "every capture's outcome flag is cross-checked: DOM suffix/indicator must agree with the policy role status or the capture fails"
      pattern: "source-policy"
    - from: "scripts/phase-01-gate.mjs"
      to: "scripts/verify-clean-checkout.sh"
      via: "the gate consumes the run_id and tested SHA written as the log's first line and refuses mismatch or staleness"
      pattern: "clean-checkout"
    - from: "package.json"
      to: "scripts/phase-01-gate.mjs"
      via: "root scripts verify:clean:phase-01, gate:phase-01, signoff:phase-01, test:smoke:ui, test:a11y wired explicitly"
      pattern: "gate:phase-01"
---

<objective>
Build and commit the final-evidence machinery: the UI-SPEC evidence/a11y harness whose honesty flags are read from the rendered DOM and cross-checked against the source policy, and the complete Plan 09 gate tooling (SHA-bound clean-checkout runner, 13-check machine gate with generated EVIDENCE-INDEX, and the mechanically fail-closed sign-off script) with unit tests — then generate every non-final evidence artifact from the built artifact, measure the real quick/full runner runtimes, and record them in 01-VALIDATION.md as observed values.

Purpose: the review's strongest findings were fail-open verification — stale evidence, semicolon-chained commands, harness-authored honesty flags, prose-only sign-off guards, an unmeasured runtime claim. This plan builds the tooling that makes those failures mechanically impossible, tests the tooling itself, and replaces claims with measurements. Plan 09 then only RUNS committed tooling — it creates nothing.
Output: ui-evidence + a11y harnesses with manifest cases, verify-clean-checkout.sh + phase-01-gate.mjs + phase-01-signoff.mjs + their unit tests, root script wiring, all 15-state UI/a11y evidence committed, runtime measurements, and the VALIDATION runtime cells updated with observed values. Zero installs; zero lockfile mutations.
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-07-SUMMARY.md
@apps/napplet/tests/smoke/paja-harness.mjs
@apps/napplet/tests/smoke/cases.json
@.planning/evidence/phase-01/local-surface-decision.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI evidence + a11y harness with DOM-derived honesty, and the complete gate/signoff tooling with schema/freshness tests — committed before any evidence generation</name>
  <files>apps/napplet/tests/smoke/ui-evidence-harness.mjs, apps/napplet/tests/smoke/a11y-contract.mjs, apps/napplet/tests/smoke/paja-harness.mjs, apps/napplet/tests/smoke/cases.json, scripts/verify-clean-checkout.sh, scripts/phase-01-gate.mjs, scripts/phase-01-signoff.mjs, scripts/phase-01-evidence-map.json, scripts/tests/gate-tooling.test.mjs, package.json</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md (Evidence & Visual Test Requirements — the 15 numbered captures; Accessibility Contract; UI Considerations long-text backstop), apps/napplet/tests/smoke/paja-harness.mjs, apps/napplet/tests/smoke/cases.json, .planning/evidence/phase-01/package-audit.md (approved pin list for the gate's dependency check)</read_first>
  <precondition>Plan 07 complete: the manifest ladder is green including the four negative cases and the local-surface hard gate; the tree is committed. No install runs in this plan.</precondition>
  <action>
  UI EVIDENCE HARNESS — `apps/napplet/tests/smoke/ui-evidence-harness.mjs`, reusing the paja-harness boot/launch machinery (direct playwright import, built artifact only): capture the 15 UI-SPEC states under `.planning/evidence/phase-01/ui/` — (1) empty first-run desktop AND 320px; (2) drawing in progress; (3) selected valid with area + populated DTO; (4) the five validation error classes (malformed, out-of-range, order, antimeridian, area-too-large) via the coordinate fields; (5) preview loading; (6) preview ready; (7) resource denied with Retry preview visible; (8) resource timeout; (9) source unavailable; (10) offline/degraded banner; (11) clear + undo toast with restore verified; (12) coordinate-entry keyboard-only bbox creation with visible focus ring; (13) 2x DPR capture; (14) reduced-motion run (toast persistent, no spinner); (15) forced-colors spot check. Add the long-attribution backstop: at 320px, an attribution string at the policy's real length wraps to at most 2 caption lines without covering map controls. Each capture writes a JSON record with screenshot path, viewport/DPR/media flags, and — for imagery-bearing states — an `outcome` field READ FROM THE RENDERED DOM (source-row suffix text "— live" / "— test fixture" / "— unavailable" plus the indicator element's live/neutral state), then cross-checked against `apps/napplet/src/config/source-policy.json` role status; a DOM/policy disagreement FAILS the capture. The harness never writes an outcome it decided itself. Extend `paja-harness.mjs` minimally so the preview port comes from env `TERRDVM_PREVIEW_PORT` (default 4173) — required so a clean-checkout run cannot collide with a working-tree run.

  A11Y HARNESS — `apps/napplet/tests/smoke/a11y-contract.mjs`: dependency-free rendered-DOM checks executed in the same Paja session (axe-core is not an approved pin and is not installed): computed-style contrast of RENDERED text/background pairs for every UI-SPEC color pair; :focus-visible ring (computed outline) on every interactive element reached by Tab in the contract's order; skip link first-focusable; aria-live="polite" status region and role="alert" banner present and exercised by the state transitions driven in this run; real label association on all four coordinate inputs; DTO panel rendered as a dl; badge visually-hidden expansions present; 44px minimum hit targets measured from bounding boxes; reduced-motion disables the spinner and toast fade. Results to `.planning/evidence/phase-01/a11y-contract.json` including a `checker_provenance` block naming the in-repo script path and method ("rendered-DOM computed-style checks, not axe-core") so the UI-SPEC's "axe-core or equivalent" claim stays auditable.

  APPEND manifest entries to `cases.json`: `ui-states` (script ui-evidence-harness.mjs) and `a11y-contract` (script a11y-contract.mjs) — the runner picks both up with no runner edit. Wire root scripts in `package.json`: `"test:smoke:ui": "node apps/napplet/tests/smoke/ui-evidence-harness.mjs"`, `"test:a11y": "node apps/napplet/tests/smoke/a11y-contract.mjs"`, `"verify:clean:phase-01": "bash scripts/verify-clean-checkout.sh"`, `"gate:phase-01": "node scripts/phase-01-gate.mjs --require-pass"`, `"signoff:phase-01": "node scripts/phase-01-signoff.mjs"`, `"test:gate-tooling": "node --test scripts/tests/gate-tooling.test.mjs"`.

  Create checked-in `scripts/phase-01-evidence-map.json` with exactly 16 keys: the 14 owned Phase 1 requirement IDs plus VER-04-T1 and VER-04-T3. Each key declares the exact non-empty evidence path(s), producer task, and machine predicate. MAP-03, MAP-04, and MAP-07 map to dedicated `.planning/evidence/phase-01/requirements/<ID>.json` records generated in Task 2 from their named test-command output plus the corresponding built-UI capture/DTO observation; the remaining rows map to their named Paja, policy, provenance, conformance, security, ledger, or dist evidence. The gate reads only this checked-in map, rejects missing/extra keys and missing/empty backing files, and generates EVIDENCE-INDEX from it rather than inventing mappings.

  GATE TOOLING (created and fixture-tested now, RUN against the real tree only in Plan 09 — Plan 09 executes committed, tested code and creates nothing):

  `scripts/verify-clean-checkout.sh` (`set -euo pipefail`, every step fail-closed): (a) abort unless `git status --porcelain` is empty — preflight cleanliness is a hard precondition, never a warning; (b) capture `SHA=$(git rev-parse HEAD)` and a `run_id` (SHA + RFC3339 start time) and write them as the FIRST line of `.planning/evidence/phase-01/clean-checkout.log`; (c) `git worktree add --detach <tmp path outside the repo> "$SHA"` — the worktree is created at that exact SHA, never at a moving ref; (d) assert the required committed artifacts exist IN THE WORKTREE, failing closed on any missing: scripts/verify-phase-01.sh, scripts/phase-01-gate.mjs, apps/napplet/src/config/source-policy.json, .planning/evidence/phase-01/package-audit.md, apps/napplet/tests/smoke/a11y-contract.mjs, apps/napplet/tests/smoke/cases.json; (e) run Corepack-pinned `pnpm install --frozen-lockfile` in the worktree; (f) export `TERRDVM_PREVIEW_PORT` to a fixed alternate port (4273) unique to clean runs; (g) run the FULL manifest suite `bash scripts/verify-phase-01.sh` in the worktree; (h) copy the worktree's conformance/Paja/gate-relevant evidence back under `.planning/evidence/phase-01/clean/` before teardown; (i) append the finish timestamp and per-step statuses to the log. Any step failure exits non-zero immediately — no partial-success state exists.

  `scripts/phase-01-gate.mjs` (plain Node): recomputes and records exactly 13 named checks into `.planning/evidence/phase-01/gate-verdict.json` — (1) tested-sha-binding: clean-checkout.log first-line SHA equals the verdict's tested_sha and the log records completion for that run_id; (2) preflight-clean: the log proves the preflight porcelain check passed; (3) output-only-dirty: current `git status --porcelain` names only the declared gate evidence outputs; (4) current-tree-bound: HEAD equals tested_sha, OR HEAD's diff from tested_sha touches only the declared gate evidence outputs (the output-only commit rule); (5) requirement-evidence: all 14 Phase 1 requirement IDs plus VER-04-T1 and VER-04-T3 map to existing non-empty evidence files via `phase-01-evidence-map.json`; (6) source-roles-live: `verify-source-policy.mjs --require-live` exits 0 (both roles live-verified); (7) live-outcome-derived: paja-preview-ready-live.json and the ui-states captures carry DOM-derived outcomes that agree with policy status, with bbox correlation recorded true; (8) deps-approved: `pnpm ls -r --depth 0 --json` cross-checked against the package-audit pin list — never regex-parsing pnpm-lock.yaml; (9) dist-no-remote: verify-dist.mjs green on a fresh build inventory (no remote script/style/font/worker/image reference); (10) secret-public-diff: scan-secrets exit 0 and `git diff --check` clean; (11) sbox05-branch: local-surface-decision.json consistent with the wired branch — verify:no-local-surface green, and test:local-resource-boundary green if and only if the fallback branch is recorded; (12) ledger-valid: validate-fallback-ledger green and every recorded >30-minute blocker has an entry; (13) no-phase2-surface: comment-filtered static scan finds no signing/relay/invoice/payment/artifact-server/retrieval-endpoint code path and the installed dependency set contains no Phase 2 package. The verdict JSON carries `run_id`, `generated_at` (RFC3339), `tested_sha`, `allowed_outputs` (exactly clean-checkout.log, clean/*, gate-verdict.json, EVIDENCE-INDEX.md, public-diff-review.md, and the Phase 1 VALIDATION.md status file), `checks` (13 objects with id/status/evidence), and `verdict` (PASS only when all 13 PASS; else FAIL). Normal/`--require-pass` mode writes the fresh verdict and generated index but MUST preserve any existing valid `human_signoff` block byte-for-byte when recomputation still passes. `--check-only --require-pass` recomputes in memory, writes no file, and exits non-zero on any mismatch/FAIL/staleness; this is the post-commit/signoff verifier. The script GENERATES `.planning/evidence/phase-01/EVIDENCE-INDEX.md` from the checked-in evidence map and validated backing paths with exactly 16 rows, never reads a hand-authored index.

  `scripts/phase-01-signoff.mjs`: refuses to record `human_signoff: "approved"` unless ALL of — it recomputes the gate checks and the fresh verdict is PASS; the verdict's run_id matches clean-checkout.log; the current tree is clean; and HEAD's diff from tested_sha touches only `allowed_outputs`. On any FAIL, staleness, SHA mismatch, dirty tree, or unexpected diff it exits non-zero naming the refusal reason and writes nothing. On success it writes the sign-off block (who/when/resume value) into gate-verdict.json and completes the public-diff-review artifact.

  `scripts/tests/gate-tooling.test.mjs` (node --test, zero new packages) against synthetic fixture verdict/log files in a temp directory: a verdict missing generated_at or run_id is rejected; a checks array whose length is not 13 is rejected; the checked-in evidence map has exactly the 16 required keys with no extras; verdict FAIL under --require-pass exits non-zero; `--check-only --require-pass` is byte-for-byte read-only; a passing recomputation preserves an existing human_signoff block; a run_id not matching the log's first line is rejected as stale; signoff refuses on FAIL verdict, on dirty tree, and on a diff containing a path outside allowed_outputs.

  COMMIT everything in this task before Task 2 runs — Task 2 and Plan 09 consume only committed tooling.
  </action>
  <verify>
    <automated>pnpm test:gate-tooling && bash -n scripts/verify-clean-checkout.sh && node -e "const p=require('./package.json'); for(const s of ['test:smoke:ui','test:a11y','verify:clean:phase-01','gate:phase-01','signoff:phase-01','test:gate-tooling']) if(!p.scripts[s]) process.exit(1)" && node -e "const m=require('./apps/napplet/tests/smoke/cases.json'); for(const id of ['ui-states','a11y-contract']) if(!m.cases.some(c=>c.id===id)) process.exit(1)" && node -e "const m=require('./scripts/phase-01-evidence-map.json'); const ids=['MAP-01','MAP-02','MAP-03','MAP-04','MAP-05','MAP-06','MAP-07','MAP-08','SBOX-01','SBOX-02','SBOX-03','SBOX-04','VER-02','OPS-01','VER-04-T1','VER-04-T3']; if(Object.keys(m).length!==16||ids.some(id=>!m[id])) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - gate-tooling tests pass: schema, check-count (13), freshness, --require-pass, and all signoff refusal paths proven on fixtures
    - verify-clean-checkout.sh parses (bash -n) and contains the preflight-clean abort, first-line SHA/run_id, worktree-at-SHA, required-artifact assertions, frozen install, TERRDVM_PREVIEW_PORT export, and evidence copy-back
    - All six root scripts are wired; ui-states and a11y-contract entries exist in the manifest; the harness reads TERRDVM_PREVIEW_PORT
    - Every automated command in this plan chains with && only — no semicolon-chained file-existence fallback appears in any script or verify block
    - This task's files are committed before Task 2 starts
  </acceptance_criteria>
  <done>The complete evidence and gate machinery exists, is unit-tested against fixtures, and is committed: DOM-derived UI honesty, provenance-recorded a11y checks, SHA-bound fail-closed clean checkout, a 13-check gate that generates the 16-row EVIDENCE-INDEX, and a sign-off script that cannot approve a FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: Generate all non-final UI/a11y evidence from the built artifact, run the full working-tree ladder, and record measured runtimes in VALIDATION</name>
  <files>.planning/evidence/phase-01/ui/*, .planning/evidence/phase-01/a11y-contract.json, .planning/evidence/phase-01/runtime-measurements.json, .planning/evidence/phase-01/requirements/*, .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md (Test Infrastructure runtime cells, Sampling Rate), apps/napplet/tests/smoke/cases.json</read_first>
  <precondition>Task 1 committed; `git status --porcelain` shows no uncommitted tooling.</precondition>
  <action>
  Build fresh (`pnpm --filter @terrdvm/napplet build`), then generate the full evidence set from the BUILT artifact: run the complete manifest ladder `bash scripts/verify-phase-01.sh` — which now executes every smoke case including ui-states and a11y-contract — capturing wall-clock time. All UI captures land under `.planning/evidence/phase-01/ui/` with their DOM-derived outcome records; a11y results land in a11y-contract.json with provenance. Materialize every row required by `scripts/phase-01-evidence-map.json`; in particular write dedicated MAP-03/MAP-04/MAP-07 JSON records from the named unit-command output and cross-link each to the built selected/error/DTO capture. Redact everything per the evidence README policy (no tokens, no user-identifying paths) and rescan with `pnpm scan:secrets`.

  Measure runtimes honestly: (a) quick runner — time `pnpm --filter @terrdvm/napplet test:unit` (three runs, record median); (b) full runner — time `bash scripts/verify-phase-01.sh` end-to-end on the working tree (record the single full measured run; note that the clean-checkout gate is a separate command whose cold frozen install is EXCLUDED from both targets). Write `.planning/evidence/phase-01/runtime-measurements.json` with `quick_runner_seconds`, `full_runner_seconds`, run timestamps, and machine context (CPU count only — no identifying hardware serials).

  Update `01-VALIDATION.md` ONLY in these places: the "Estimated runtime" infrastructure cell and the Sampling Rate latency line — replace the "measure in Plan 08 Task 2; update with observed values" placeholders with the observed numbers from runtime-measurements.json (state them as measured values with the measurement date; if the full runner exceeds prior expectations, the honest number stands — do not tune assertions to fit a target). Change no other VALIDATION content, no task IDs, no frontmatter flags (status/nyquist_compliant/wave_0_complete remain as they are; the plan checker owns those).

  Commit all evidence, measurements, and the VALIDATION runtime edit by the end of this task — Plan 09's machine gate requires a clean committed tree containing this evidence.
  </action>
  <verify>
    <automated>bash scripts/verify-phase-01.sh && node -e "const r=require('./.planning/evidence/phase-01/runtime-measurements.json'); if(!(r.quick_runner_seconds>0&&r.full_runner_seconds>0)) process.exit(1)" && pnpm scan:secrets && node -e "const fs=require('fs'); const v=fs.readFileSync('.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md','utf8'); if(v.includes('measure in Plan 08 Task 2')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The full ladder passes on the working tree with ui-states and a11y-contract executed from the manifest; all 15 UI states plus variant captures exist with DOM-derived outcomes agreeing with the source policy
    - runtime-measurements.json holds real measured values; 01-VALIDATION.md runtime cells show those observed values (measurement-dated) and no unmeasured runtime claim remains
    - All new evidence is redacted and scan-clean; everything is committed by task end (git status clean)
  </acceptance_criteria>
  <done>Every non-final evidence artifact exists, honest and committed: 15-state UI evidence with DOM-derived honesty, provenance-recorded a11y results, and measured quick/full runtimes recorded in VALIDATION in place of claims.</done>
</task>

<task type="auto">
  <name>Task 3: Fail-open regression tests — semicolon-chained verify patterns forbidden, stale verdicts rejected, FAIL unsignable, allowed-diff logic proven</name>
  <files>scripts/tests/gate-tooling.test.mjs, package.json</files>
  <read_first>scripts/phase-01-gate.mjs, scripts/phase-01-signoff.mjs, scripts/verify-clean-checkout.sh, scripts/tests/gate-tooling.test.mjs</read_first>
  <precondition>Tasks 1-2 committed and the ladder green.</precondition>
  <action>
  Extend `scripts/tests/gate-tooling.test.mjs` with a bounded fail-open regression suite (no new files beyond this extension, no new packages):

  - Static hygiene scan: read every file under `scripts/` plus the `scripts` block of both package.json manifests and assert no verification command chains a file-existence check after a semicolon in a way that discards a prior exit code (match the semicolon-then-`test -s`/`test -f` shape on non-comment lines) — the exact fail-open pattern the review found. The scan is region-scoped to scripts and package manifests and filters comment lines so documentation cannot self-trigger it.
  - Staleness regression: construct a fixture where gate-verdict.json is valid but its run_id references a previous clean-checkout.log first line; assert --require-pass exits non-zero.
  - FAIL-unsignable regression: fixture verdict with 12 passing checks and 1 failing; assert verdict is FAIL, --require-pass exits non-zero, and phase-01-signoff.mjs refuses with a named reason and writes nothing (fixture gate-verdict.json byte-identical after the refused attempt).
  - Allowed-diff logic: fixture repositories (temp dirs with git init) where (a) HEAD equals tested_sha → signoff diff check passes; (b) HEAD differs from tested_sha only in allowed_outputs paths → passes; (c) HEAD differs in one extra source file → signoff refuses; (d) dirty working tree → signoff refuses before any diff evaluation.
  - EVIDENCE-INDEX shape: run the generator against a passing fixture and assert exactly 16 rows, one per requirement ID and VER-04 case, each with a non-empty evidence path.

  Keep the suite bounded: fixtures are generated in-test into temp directories and cleaned up; total added runtime must stay within a few seconds (it is part of the quick tooling suite, not the browser ladder). Wire nothing new — `pnpm test:gate-tooling` already runs the file.
  </action>
  <verify>
    <automated>pnpm test:gate-tooling && bash scripts/verify-phase-01.sh</automated>
  </verify>
  <acceptance_criteria>
    - The hygiene scan passes on the real repository and demonstrably fails on an injected probe line containing the forbidden semicolon-chained shape (prove once in a fixture, not in committed scripts)
    - Staleness, FAIL-unsignable, allowed-diff (all four branches), and 16-row index tests pass
    - The full ladder remains green and no lockfile delta exists
  </acceptance_criteria>
  <done>The gate cannot silently rot: fail-open command shapes, stale verdicts, FAIL sign-offs, and out-of-scope diffs are each pinned by a regression test that runs in the quick tooling suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → evidence honesty flags | An outcome authored by the harness rather than the DOM would fake the phase's headline claim |
| Gate tooling → phase completion | Fail-open tooling would let a broken or stale state certify the phase |
| Evidence + captures → public GitHub | Screenshots, logs, and measurements are published redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-41 | Spoofing | Harness-authored live/fixture outcome | high | mitigate | Outcome read from rendered DOM suffix/indicator and cross-checked against source-policy status; disagreement fails the capture |
| T-01-42 | Tampering | Stale or fail-open gate evidence | high | mitigate | run_id/generated_at freshness binding to the clean-checkout log; && -only chains; regression tests for the semicolon shape and staleness |
| T-01-43 | Repudiation | Sign-off recorded over a machine FAIL | high | mitigate | phase-01-signoff.mjs recomputes the gate and refuses FAIL/stale/dirty/unexpected-diff; refusal paths unit-tested |
| T-01-44 | Information Disclosure | Captures or measurements leaking paths/tokens | medium | mitigate | Redaction per evidence README; scan:secrets over all new evidence; machine context limited to CPU count |
| T-01-45 | Tampering | A11y/contrast claims not matching rendered reality | medium | mitigate | Checks computed from rendered DOM styles with recorded provenance, not from token literals alone |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero installs in this plan; lock-approved ladder step; public-diff review at wave end |
</threat_model>

<verification>
- [ ] `pnpm test:gate-tooling` green including all fail-open regression paths
- [ ] `bash scripts/verify-phase-01.sh` green with ui-states and a11y-contract executed from the manifest
- [ ] runtime-measurements.json committed and 01-VALIDATION.md runtime cells show observed values only
- [ ] Wave-end secret/public-diff gate: `pnpm scan:secrets` exits 0 over repo + dist + new evidence; `git diff --check` clean; no lockfile delta in this plan's commits
</verification>

<success_criteria>
- All 15 UI-SPEC states plus variants captured from the built artifact with DOM-derived, policy-cross-checked honesty; a11y contract asserted mechanically with recorded provenance
- Gate/clean-checkout/signoff tooling committed and unit-tested before use, with staleness, FAIL-unsignable, and allowed-diff logic proven on fixtures
- Quick and full runtimes measured and recorded in VALIDATION as observed values; all non-final evidence committed
</success_criteria>

<output>
Create `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-08-SUMMARY.md` when done
</output>
