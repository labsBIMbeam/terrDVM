---
phase: 01-sandboxed-bbox-and-orthophoto-ui
plan: 09
type: execute
wave: 9
depends_on: ["01-08"]
files_modified:
  - .planning/evidence/phase-01/clean-checkout.log
  - .planning/evidence/phase-01/clean/*
  - .planning/evidence/phase-01/gate-verdict.json
  - .planning/evidence/phase-01/EVIDENCE-INDEX.md
  - .planning/evidence/phase-01/public-diff-review.md
  - .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md
autonomous: false
requirements: [MAP-01, MAP-02, MAP-03, MAP-04, MAP-05, MAP-06, MAP-07, MAP-08, SBOX-01, SBOX-02, SBOX-03, SBOX-04, VER-02, OPS-01]
must_haves:
  truths:
    - "The machine verdict is produced from a clean checkout at one exact captured HEAD SHA: verify-clean-checkout.sh aborts on a dirty preflight tree, writes the SHA and run_id as the log's FIRST line, builds a detached worktree at that exact SHA (never a moving ref), asserts every required committed artifact exists in the worktree, runs a frozen install and the FULL manifest-driven suite on an isolated preview port, and exits non-zero on any step — no partial-success state exists"
    - "phase-01-gate.mjs --require-pass certifies all 13 named checks — SHA binding, preflight cleanliness, output-only dirty state, current-tree binding, all-14-requirement plus VER-04-T1/T3 evidence, both source roles live, DOM-derived bbox-correlated live outcome, pnpm-ls-based approved-pin cross-check, dist free of remote assets, secret/public-diff cleanliness, the recorded SBOX-05 branch, a valid OPS-01 ledger, and zero Phase 2 surface — and rejects any verdict whose run_id does not match the current clean-checkout log"
    - "EVIDENCE-INDEX.md is GENERATED by the gate with exactly 16 rows (14 requirement IDs + VER-04-T1 + VER-04-T3), each naming a non-empty evidence path and its validating check — never hand-authored, so an empty cell is structurally impossible"
    - "The human visual/public-diff checkpoint is mechanically unreachable as an approval when the committed gate verdict is not PASS: on FAIL the only valid resume values are acknowledged-blocked or a failure description and the phase remains open — no prose can flip the verdict"
    - "Sign-off is recorded exclusively by phase-01-signoff.mjs, which recomputes the gate, and refuses on FAIL, staleness, tested-SHA mismatch, a dirty tree, or any diff from the tested SHA beyond the declared gate evidence outputs — the output-only commit rule is the single permitted difference, so the tested-SHA binding survives committing this plan's own evidence"
  artifacts:
    - path: ".planning/evidence/phase-01/clean-checkout.log"
      provides: "SHA-bound clean-checkout run log; first line carries the tested HEAD SHA and run_id consumed by the gate"
      contains: "run_id"
    - path: ".planning/evidence/phase-01/gate-verdict.json"
      provides: "Machine verdict: run_id, generated_at, tested_sha, allowed_outputs, 13 named checks, verdict, and (after Task 3) the human sign-off block"
      contains: "tested_sha"
    - path: ".planning/evidence/phase-01/EVIDENCE-INDEX.md"
      provides: "Generated 16-row index mapping every requirement ID and VER-04 case to its evidence path and validating check"
      contains: "VER-04-T3"
    - path: ".planning/evidence/phase-01/public-diff-review.md"
      provides: "Redacted human public-diff/visual review record completed by the sign-off script after the approved checkpoint"
      contains: "approved"
  key_links:
    - from: ".planning/evidence/phase-01/clean-checkout.log"
      to: "scripts/phase-01-gate.mjs"
      via: "the gate reads the log's first-line SHA/run_id and refuses any verdict not bound to the current completed run"
      pattern: "run_id"
    - from: ".planning/evidence/phase-01/gate-verdict.json"
      to: "Task 2 checkpoint"
      via: "the checkpoint's approval branch exists only when the committed verdict is PASS; FAIL leaves only acknowledged-blocked"
      pattern: "verdict"
    - from: "scripts/phase-01-signoff.mjs"
      to: ".planning/evidence/phase-01/gate-verdict.json"
      via: "sign-off recomputes the gate and writes the human block only into a fresh PASS verdict whose tested SHA differs from HEAD by allowed_outputs alone"
      pattern: "allowed_outputs"
---

<objective>
Certify Phase 1 with committed, already-tested machinery — this plan creates no scripts. Task 1 runs the SHA-bound fail-closed clean-checkout suite and the 13-check machine gate with `--require-pass`, producing the generated 16-row EVIDENCE-INDEX and a fresh gate-verdict bound to the exact tested SHA, then commits ONLY the declared gate evidence outputs so the tested-SHA binding survives (the output-only commit rule recorded in the verdict). Task 2 is the blocking human visual/public-diff checkpoint, reachable as an approval only over a committed PASS. Task 3 records sign-off through phase-01-signoff.mjs, which mechanically refuses any FAIL, stale verdict, or unexpected diff, then rescans and re-proves PASS.

Purpose: the review's core demand — phase completion must be impossible to assert over stale evidence, a dirty tree, an unproven SHA, or a machine FAIL. Every guard here is a committed, unit-tested script from Plan 08; the human contributes judgment (imagery semantics, honesty, public diff), never the verdict.
Output: clean-checkout.log with copied-back clean-run evidence, gate-verdict.json (PASS + sign-off block), the generated EVIDENCE-INDEX.md, and the completed public-diff-review.md. Zero installs in the working tree (the frozen install happens inside the disposable worktree); zero lockfile mutations; zero new scripts.
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
@$HOME/.hermes/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-08-SUMMARY.md
@scripts/verify-clean-checkout.sh
@scripts/phase-01-gate.mjs
@scripts/phase-01-signoff.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MACHINE GATE — SHA-bound clean-checkout suite, 13-check gate with --require-pass, generated 16-row EVIDENCE-INDEX, then the gate-evidence-only commit</name>
  <files>.planning/evidence/phase-01/clean-checkout.log, .planning/evidence/phase-01/clean/*, .planning/evidence/phase-01/gate-verdict.json, .planning/evidence/phase-01/EVIDENCE-INDEX.md</files>
  <read_first>scripts/verify-clean-checkout.sh, scripts/phase-01-gate.mjs, .planning/evidence/phase-01/package-audit.md, .planning/evidence/phase-01/local-surface-decision.json</read_first>
  <precondition>Plan 08 is complete and committed: `git status --porcelain` is empty, `pnpm test:gate-tooling` is green, and all evidence from Plans 03-08 is in HEAD. This plan runs committed tooling only — if any of scripts/verify-clean-checkout.sh, scripts/phase-01-gate.mjs, or scripts/phase-01-signoff.mjs is missing or uncommitted, halt; do not create or patch it here.</precondition>
  <action>
  Run the committed clean-checkout suite: `bash scripts/verify-clean-checkout.sh` (root alias `pnpm verify:clean:phase-01`). Per its committed contract it (a) aborts unless the preflight tree is clean; (b) captures `SHA=$(git rev-parse HEAD)` plus a run_id and writes them as the FIRST line of `.planning/evidence/phase-01/clean-checkout.log`; (c) creates a detached git worktree at that exact SHA; (d) asserts the required committed artifacts exist in the worktree — scripts/verify-phase-01.sh, scripts/phase-01-gate.mjs, apps/napplet/src/config/source-policy.json, .planning/evidence/phase-01/package-audit.md, apps/napplet/tests/smoke/a11y-contract.mjs, apps/napplet/tests/smoke/cases.json — failing closed on any absence; (e) runs the Corepack-pinned frozen install in the worktree; (f) exports the clean-run TERRDVM_PREVIEW_PORT (4273) so the Paja port never collides with a working-tree run; (g) runs the FULL manifest-driven suite in the worktree; (h) copies the clean-run evidence back under `.planning/evidence/phase-01/clean/`; (i) logs per-step statuses and the finish timestamp. Do not edit the script, skip a step, reuse a log from an earlier run, or continue past a non-zero exit — a failure here means the phase is not done, and the honest next step is a gap plan, not a rerun-until-green loop (rerunning is legitimate only after a recorded environmental cause, noted in the SUMMARY).

  Then run the committed gate: `node scripts/phase-01-gate.mjs --require-pass` (root alias `pnpm gate:phase-01`). It recomputes the 13 named checks into `.planning/evidence/phase-01/gate-verdict.json`: (1) tested-sha-binding to the log's first line and completed run_id; (2) preflight-clean proven by the log; (3) output-only-dirty — current porcelain names only the declared gate evidence outputs; (4) current-tree-bound — HEAD equals tested_sha or differs only in allowed_outputs; (5) requirement-evidence for all 14 Phase 1 IDs plus VER-04-T1 and VER-04-T3; (6) source-roles-live via `node scripts/verify-source-policy.mjs --require-live`; (7) live-outcome-derived — preview and ui-states outcomes read from the rendered DOM, agreeing with policy, bbox correlation true; (8) deps-approved via `pnpm ls -r --depth 0 --json` against the package-audit pin list (never regex-parsing the lockfile); (9) dist-no-remote; (10) secret-public-diff; (11) sbox05-branch consistent with local-surface-decision.json; (12) ledger-valid; (13) no-phase2-surface. The verdict carries run_id, generated_at, tested_sha, allowed_outputs, and 13 check entries; PASS requires all 13. The gate GENERATES `.planning/evidence/phase-01/EVIDENCE-INDEX.md` with exactly 16 rows (14 requirement IDs + VER-04-T1 + VER-04-T3), each naming its evidence path and validating check. `--require-pass` exits non-zero on any FAIL or on a run_id that does not match the current log — a stale verdict cannot certify anything.

  After BOTH commands exit 0, make the gate-evidence-only commit: stage exactly the generated paths (clean-checkout.log, clean/ copy-back, gate-verdict.json, EVIDENCE-INDEX.md) and commit them, touching nothing else. This is the output-only commit rule the verdict itself records: after the commit, HEAD differs from tested_sha only in allowed_outputs, so gate check 4 and the sign-off diff check still bind the certified state to the tested SHA. Confirm read-only with `node scripts/phase-01-gate.mjs --check-only --require-pass`, then assert `git status --porcelain` is empty. Do not rerun the stateful clean-checkout or writing gate after the output commit.
  </action>
  <verify>
    <automated>node scripts/phase-01-gate.mjs --check-only --require-pass && test -z "$(git status --porcelain)" && node -e "const v=require('./.planning/evidence/phase-01/gate-verdict.json'); if(!(v.run_id&&v.generated_at&&v.tested_sha&&Array.isArray(v.allowed_outputs)&&Array.isArray(v.checks)&&v.checks.length===13&&v.verdict==='PASS')) process.exit(1)" && node -e "const fs=require('fs'); const idx=fs.readFileSync('.planning/evidence/phase-01/EVIDENCE-INDEX.md','utf8'); const rows=idx.split('\n').filter(l=>l.startsWith('|')&&!/^\|[\s|:-]+\|?$/.test(l)&&!/Requirement/i.test(l)); if(rows.length!==16) process.exit(1); for(const id of ['MAP-01','MAP-02','MAP-03','MAP-04','MAP-05','MAP-06','MAP-07','MAP-08','SBOX-01','SBOX-02','SBOX-03','SBOX-04','VER-02','OPS-01','VER-04-T1','VER-04-T3']) if(!idx.includes(id)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - clean-checkout.log's first line carries the tested HEAD SHA and run_id, records the preflight-clean pass, the worktree at that exact SHA, all required-artifact assertions, the frozen install, the isolated preview port, and full-suite completion; clean-run evidence exists under .planning/evidence/phase-01/clean/
    - gate-verdict.json holds exactly 13 named checks, all passing, with run_id matching the current log; EVIDENCE-INDEX.md is gate-generated with exactly 16 rows and no empty evidence path
    - Every automated command chains with && only; no semicolon-chained file-existence fallback appears anywhere in this task
    - The gate-evidence-only commit contains exclusively the allowed_outputs paths, and --require-pass still exits 0 afterward (current-tree-bound holds under the output-only commit rule)
  </acceptance_criteria>
  <done>A fresh machine PASS exists, bound to one exact tested SHA through a fail-closed clean checkout, with the 16-row generated EVIDENCE-INDEX, and the verdict plus its evidence are committed under the output-only rule without breaking that binding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING visual/public-diff checkpoint — human judgment over a committed machine PASS, never instead of it</name>
  <precondition>The committed gate-verdict.json from Task 1 exists in HEAD. Read its verdict before presenting this checkpoint: the approval branch below is only offered when it is PASS; on FAIL the executor states the failing checks and offers only the blocked outcomes.</precondition>
  <what-built>Task 1 produced and committed a machine PASS: the full suite ran in a clean worktree at the exact tested SHA, all 13 gate checks passed, and the 16-row EVIDENCE-INDEX maps every Phase 1 requirement plus VER-04-T1/T3 to concrete evidence. This checkpoint covers ONLY what automation cannot: imagery semantics, honesty of presentation, and contextual public-diff review. It cannot override the machine: if the committed verdict were FAIL you would not be offered an approval option at all, and the sign-off script in Task 3 will independently refuse anything but a fresh PASS.</what-built>
  <how-to-verify>
    1. Confirm the committed verdict: open `.planning/evidence/phase-01/gate-verdict.json` — verdict PASS, 13/13 checks, run_id matching the first line of clean-checkout.log, tested_sha equal to the parent of the gate-evidence commit.
    2. Imagery semantics (MAP-05): in the built Paja app, draw a recognizable bounded area and confirm the live orthophoto preview actually corresponds to the selected bbox — visible landmarks/bounds match the source reference, not merely a correlated request ID. Compare against the ui-states live capture.
    3. Attribution/source honesty (MAP-06/MAP-07): confirm the rendered attribution is legible at 320px and desktop, the source label and live/fixture/unavailable indicator state truthfully reflect the two-role source policy, and no capture presents fixture imagery as live.
    4. Captures: spot-check the 15-state UI evidence under `.planning/evidence/phase-01/ui/` and the a11y-contract.json provenance — screenshots match the recorded states and everything is redacted.
    5. Lockfile/audit: confirm the dependency tree still matches the approved pin list in package-audit.md (the gate's deps-approved check names the pnpm-ls evidence) and no unexplained package appears anywhere in the phase's commits.
    6. Public diff: review the full phase diff as it will appear publicly — source, lockfile, dist inventory, evidence, logs, traces, config — for unsafe evidence, secret-like material, or anything misleading.
  </how-to-verify>
  <resume-signal>If the committed verdict is PASS: type "approved" (Task 3 records sign-off mechanically) or describe the failure you found (phase stays open; a gap plan follows). If the committed verdict is FAIL: the only valid resumes are "acknowledged-blocked" or a failure description — approval is not offered and the phase remains open.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record sign-off through phase-01-signoff.mjs — mechanical refusal of FAIL/stale/unexpected-diff, final rescan, final PASS</name>
  <files>.planning/evidence/phase-01/gate-verdict.json, .planning/evidence/phase-01/public-diff-review.md, .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md</files>
  <read_first>scripts/phase-01-signoff.mjs, .planning/evidence/phase-01/gate-verdict.json</read_first>
  <precondition>Task 2 resumed with "approved". Any other resume value (acknowledged-blocked or a failure description) means this task must not run; the SUMMARY records the open phase state instead.</precondition>
  <action>
  Record the sign-off exclusively via the committed script: `node scripts/phase-01-signoff.mjs` (root alias `pnpm signoff:phase-01`), passing the checkpoint resume value and reviewer identity per its committed interface. The script — not this task's prose — enforces every condition: it recomputes the gate checks and requires a fresh PASS; requires the verdict run_id to match the current clean-checkout.log first line; requires a clean working tree; and permits HEAD to differ from tested_sha ONLY in the verdict's allowed_outputs (the gate-evidence commit from Task 1 plus this task's own declared outputs). On any machine FAIL, stale verdict, tested-SHA mismatch, dirty tree, or unexpected diff it exits non-zero naming the refusal reason and writes NOTHING — do not edit gate-verdict.json, public-diff-review.md, or any evidence file by hand under any circumstance; a refusal is a phase blocker to surface, not a formatting problem to work around.

  On success the script writes the sign-off block (who, when, resume value) into gate-verdict.json and completes `.planning/evidence/phase-01/public-diff-review.md` with the reviewed scope from the Task 2 checklist. Only after that machine PASS plus human sign-off, update `01-VALIDATION.md` to `status: approved` and `wave_0_complete: true`; `nyquist_compliant` was already set by the independent plan checker and is not rewritten here. The script/gate must verify every mapped infrastructure command exists and ran before this flag change. Then rescan and re-prove: `pnpm scan:secrets` over the repository including all new evidence, `git diff --check` clean, and a final read-only `node scripts/phase-01-gate.mjs --check-only --require-pass` confirming the signed verdict still passes while preserving the sign-off block. Commit the sign-off outputs and Validation status update (all declared allowed outputs) as the phase's final commit.
  </action>
  <verify>
    <automated>node -e "const v=require('./.planning/evidence/phase-01/gate-verdict.json'); if(!(v.verdict==='PASS'&&v.human_signoff&&v.human_signoff.resume==='approved'&&v.human_signoff.who&&v.human_signoff.when)) process.exit(1)" && node scripts/phase-01-gate.mjs --check-only --require-pass && pnpm scan:secrets && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The sign-off block exists in gate-verdict.json written by phase-01-signoff.mjs only; no hand edit touched any evidence file
    - public-diff-review.md records the completed visual/attribution/lockfile/public-diff review, redacted
    - Final rescan clean and the final --require-pass exits 0 on the committed tree; the phase's entire diff from the tested SHA remains within allowed_outputs
    - If the script refused for any reason, nothing was written, the refusal reason is recorded in the SUMMARY, and the phase remains open
  </acceptance_criteria>
  <done>Phase 1 is signed off with no prose-only flip anywhere in the chain: machine PASS bound to the tested SHA, human approval recorded mechanically over that PASS, final rescan clean, and every certified byte committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → certified state | Only the clean-checkout worktree at the tested SHA is certified; the working tree may drift |
| Machine verdict → human approval | The human judges semantics and honesty; the verdict itself must be impossible to override |
| Gate evidence → public GitHub | The verdict, index, log, and review record are published redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-46 | Tampering | Stale or replayed gate verdict certifying an older tree | high | mitigate | run_id/generated_at binding to the current clean-checkout log first line; --require-pass rejects mismatch; regression-tested in Plan 08 |
| T-01-47 | Repudiation | Sign-off recorded over a machine FAIL or by hand edit | high | mitigate | phase-01-signoff.mjs is the only writer, recomputes the gate, refuses FAIL/stale/dirty/unexpected-diff; refusal paths unit-tested; final --require-pass re-proof |
| T-01-48 | Tampering | Post-gate source drift laundered into the certified state | high | mitigate | Output-only commit rule: HEAD may differ from tested_sha only in allowed_outputs, enforced by gate check 4 and the sign-off diff check |
| T-01-49 | Spoofing | Hand-authored EVIDENCE-INDEX or evidence rows without backing files | medium | mitigate | Index generated by the gate from validated evidence paths; 16-row shape and non-empty paths asserted in verify |
| T-01-50 | Information Disclosure | Clean-run logs/evidence leaking paths or tokens | medium | mitigate | Copy-back limited to gate-relevant evidence; scan:secrets over all new artifacts before the final commit |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No working-tree install; the only install is the frozen, Corepack-pinned worktree install of already-approved pins; deps-approved gate check via pnpm ls |
</threat_model>

<verification>
- [ ] `bash scripts/verify-clean-checkout.sh` completed fail-closed with the tested SHA and run_id as the log's first line and clean-run evidence copied back
- [ ] `node scripts/phase-01-gate.mjs --require-pass` exits 0 with 13/13 checks and the generated 16-row EVIDENCE-INDEX
- [ ] Checkpoint outcome recorded; approval only ever offered over a committed PASS
- [ ] Sign-off block written by phase-01-signoff.mjs only; final `pnpm scan:secrets` exits 0; `git diff --check` clean; the phase diff from the tested SHA stays within allowed_outputs; no lockfile delta in this plan's commits
</verification>

<success_criteria>
- Phase 1 completion is certified by a fresh machine PASS from a clean checkout at one exact SHA, with every requirement and the VER-04 subset mapped in a generated index
- Human judgment covered imagery semantics, presentation honesty, and the public diff — and could not have overridden, backdated, or hand-edited the machine verdict at any step
- The entire certification survives its own commits via the output-only rule, leaving no gap between the tested state and the published state
</success_criteria>

<output>
Create `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-09-SUMMARY.md` when done
</output>
