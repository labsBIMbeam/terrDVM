---
phase: 01-sandboxed-bbox-and-orthophoto-ui
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .gitignore
  - package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - eslint.config.mjs
  - apps/napplet/package.json
  - apps/napplet/tsconfig.json
  - apps/napplet/vitest.config.ts
  - apps/napplet/index.html
  - apps/napplet/src/main.ts
  - scripts/verify-lock-approved.mjs
  - .planning/evidence/phase-01/chromium-provisioning.json
  - apps/napplet/tests/smoke/cases.json
  - scripts/verify-phase-01.sh
  - scripts/scan-secrets.sh
autonomous: true
requirements: [SBOX-04, VER-02]
must_haves:
  truths:
    - "Exactly ONE lockfile mutation installs the entire human-approved Phase 1 pin list — including direct playwright@1.59.1 — and a frozen-lockfile reinstall passes; installed direct dependencies match the approved pins exactly, verified by machine"
    - "Chromium is provisioned via `playwright install chromium` only after the Plan 01 approval, with version and executable-path evidence captured; no OS-dependency install runs"
    - ".gitignore and an ESLint 9 flat config exist, and typecheck, lint, and the unit runner all execute green from the fresh workspace"
    - "verify-phase-01.sh is manifest-driven: it reads apps/napplet/tests/smoke/cases.json, runs conditional steps only when their backing script exists, and FAILS on any manifest case whose script is missing — later plans append cases without touching runner logic"
    - "An established secret scanner runs behind pnpm scan:secrets with redacted output over the repository (and dist when it exists), and the public-diff check is part of the runner"
    - "No application feature exists: no map, no bbox, no ortho, no shell adapter, no build config — this plan is scaffolding and verification infrastructure only"
  artifacts:
    - path: ".gitignore"
      provides: "Ignores node_modules/, apps/napplet/dist/, temp worktree paths, local scratch"
      contains: "node_modules"
    - path: "eslint.config.mjs"
      provides: "ESLint 9 flat config composing @eslint/js + typescript-eslint, scoped to apps/napplet and scripts/"
      contains: "typescript-eslint"
    - path: "pnpm-lock.yaml"
      provides: "The single approved Phase 1 lockfile (one mutation in git history)"
    - path: "apps/napplet/tests/smoke/cases.json"
      provides: "Smoke-case manifest, initially empty but schema-valid"
      contains: "schema_version"
    - path: "scripts/verify-phase-01.sh"
      provides: "Ordered manifest-driven phase runner with conditional steps and fail-on-missing-case-script"
      contains: "cases.json"
    - path: "scripts/verify-lock-approved.mjs"
      provides: "Cross-check of installed direct dependencies (pnpm ls JSON) against the approved pin list in package-audit.md"
    - path: "scripts/scan-secrets.sh"
      provides: "Pinned established secret scanner invocation with redacted evidence output"
    - path: ".planning/evidence/phase-01/chromium-provisioning.json"
      provides: "Playwright version and Chromium executable-path evidence from the approved provisioning run"
  key_links:
    - from: "scripts/verify-phase-01.sh"
      to: "apps/napplet/tests/smoke/cases.json"
      via: "runner enumerates and executes every manifest case, failing on missing case scripts"
      pattern: "cases\\.json"
    - from: "scripts/verify-lock-approved.mjs"
      to: ".planning/evidence/phase-01/package-audit.md"
      via: "pnpm ls -r --depth 0 --json output is cross-checked against the approved pins"
      pattern: "package-audit"
    - from: "scripts/verify-phase-01.sh"
      to: "scripts/scan-secrets.sh"
      via: "secret scan and git diff --check run as unconditional runner steps"
      pattern: "scan-secrets"
---

<objective>
Stand up the pnpm workspace with the ONE approved lockfile mutation installing the entire Phase 1 dependency list (including direct Playwright with Chromium provisioning), plus the repository hygiene the review demanded (.gitignore, ESLint 9 flat config), and build the manifest-driven verification infrastructure every later plan plugs into: a cases.json smoke manifest, a conditional ordered runner, the secret scanner, the lockfile-approval cross-check, and the public-diff check.

Purpose: the review found the runner went stale after Plan 02, ESLint had no config, Playwright was an unresolvable transitive import, and installs were split across plans. This plan makes the install atomic and pre-approved, and makes the verification ladder append-only via a manifest so later plans add cases, never runner logic.
Output: workspace + single lockfile + Chromium evidence + eslint/gitignore scaffolding + manifest, runner, scanner, and lock-approval validator. No app features; wave_0 is NOT claimed complete (build/conformance/harness arrive in Plan 03).
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md
@.planning/evidence/phase-01/package-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace scaffolding, .gitignore, ESLint 9 flat config, and the single approved lockfile mutation with Chromium provisioning</name>
  <files>.gitignore, package.json, pnpm-workspace.yaml, pnpm-lock.yaml, eslint.config.mjs, apps/napplet/package.json, apps/napplet/tsconfig.json, apps/napplet/vitest.config.ts, apps/napplet/index.html, apps/napplet/src/main.ts, scripts/verify-lock-approved.mjs, .planning/evidence/phase-01/chromium-provisioning.json</files>
  <read_first>.planning/evidence/phase-01/package-audit.md (approved pins, install locations, Chromium provisioning section), .planning/evidence/phase-01/v1-defaults.json</read_first>
  <precondition>The Plan 01 Task 3 checkpoint approval is recorded (package-audit.md approved covering ALL Phase 1 installs and the v1 defaults); `node scripts/verify-package-audit.mjs` and `node scripts/verify-v1-defaults.mjs` exit 0; node and pnpm are on PATH. If approval is absent, HALT — no install may run.</precondition>
  <action>
  Create `.gitignore` first: `node_modules/`, `apps/napplet/dist/`, `*.log` under scratch paths, `.terrcvm-worktree*/` (clean-checkout temp worktrees), editor/OS noise. Build output is never committed.

  Scaffold the workspace: root `package.json` (private, `packageManager` pinning the pnpm version observed via Corepack, `engines` gate for one supported Node major), `pnpm-workspace.yaml` with `apps/*`, and `apps/napplet/package.json` named `@terrcvm/napplet`.

  Perform ONE lockfile mutation installing EXACTLY the approved pins from `.planning/evidence/phase-01/package-audit.md` in their approved locations — write both manifests first, then run a single `pnpm install`:
  - Root devDependencies: `eslint@9.39.2`, `@eslint/js@9.39.2`, `typescript-eslint@8.46.4`.
  - apps/napplet dependencies: `@napplet/sdk@0.25.0`, `maplibre-gl@5.24.0`, `terra-draw@1.32.2`, `terra-draw-maplibre-gl-adapter@1.4.1`, `@turf/area@7.3.5`.
  - apps/napplet devDependencies: `@napplet/vite-plugin@0.12.0`, `@napplet/conformance-cli@0.2.16`, `playwright@1.59.1`, `vite@8.1.5`, `typescript@5.9.3`, `vitest@4.1.10`.
  All versions exact (no `^`/`~`). The map/geo packages are approved-installed now and first imported by later plans — that is intentional per the single-mutation decision; do NOT add anything beyond this list and do NOT run any further install command in this or any later Phase 1 plan.

  App scripts in `apps/napplet/package.json` (exact): `"dev": "vite"`, `"build": "vite build"`, `"test:unit": "vitest run"`, `"typecheck": "tsc --noEmit"` (no watch flags anywhere). Root scripts (exact): `"lint": "eslint --no-error-on-unmatched-pattern apps/napplet/src apps/napplet/tests scripts"`, `"verify:phase-01": "bash scripts/verify-phase-01.sh"`, `"scan:secrets": "bash scripts/scan-secrets.sh"`, `"verify:map-provenance": "node scripts/verify-map-provenance.mjs"`, `"verify:fallback-ledger": "node scripts/validate-fallback-ledger.mjs"`, `"verify:lock-approved": "node scripts/verify-lock-approved.mjs"`, `"test:smoke:paja": "node apps/napplet/tests/smoke/paja-harness.mjs"` (wiring only — the harness file is created by Plan 03; the script existing before its target is acceptable because nothing invokes it until the manifest has cases). The explicit ESLint unmatched-pattern guard is required because `apps/napplet/tests/` is not created until Task 2.

  `apps/napplet/tsconfig.json`: strict TypeScript 5.9, `"types": []` DOM lib browser target, includes `src` and `tests`. `apps/napplet/vitest.config.ts`: node/jsdom-free default environment, `passWithNoTests: true` (honest: no unit tests exist until Plan 03 — the flag is removed when the first real test lands), no watch. `eslint.config.mjs` at repo root: ESLint 9 FLAT config composing `@eslint/js` recommended + `typescript-eslint` recommended, scoped to `apps/napplet/src`, `apps/napplet/tests`, and `scripts/`, ignoring `dist` and `node_modules` — ESLint 9 errors without a flat config, so this file is mandatory before `lint` enters any runner.

  Minimal index: `apps/napplet/index.html` (empty semantic shell with `<div id="app">`) and `apps/napplet/src/main.ts` (a typed no-op entry that locates `#app`; no features, no adapter, no UI — Plan 03 owns all of that). These exist only so typecheck/lint have real input.

  Create `scripts/verify-lock-approved.mjs` (plain Node): runs `pnpm ls -r --depth 0 --json` (JSON — never regex-parse pnpm-lock.yaml), collects every direct dependency name@version across importers, and exits 0 only if that set is exactly a subset of the approved pins parsed from `.planning/evidence/phase-01/package-audit.md` with every approved package present in its approved location. Any extra, missing, or version-drifted package exits 1 named.

  Chromium provisioning (authorized by the Plan 01 approval): run `pnpm --filter @terrcvm/napplet exec playwright install chromium`. Do NOT run `--with-deps` or any OS package installation — that is explicitly outside the recorded approval. Capture evidence into `.planning/evidence/phase-01/chromium-provisioning.json`: playwright version (`pnpm --filter @terrcvm/napplet exec playwright --version`), the Chromium executable path (`node -e "console.log(require('playwright').chromium.executablePath())"` run from apps/napplet), and the RFC3339 provisioning timestamp. Redact any user-identifying path segments per the evidence README policy if required.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && node scripts/verify-lock-approved.mjs && pnpm --filter @terrcvm/napplet typecheck && pnpm lint && pnpm --filter @terrcvm/napplet test:unit && node -e "const c=require('./.planning/evidence/phase-01/chromium-provisioning.json'); if(!c.playwright_version||!c.chromium_path) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Git history contains exactly one commit mutating pnpm-lock.yaml; verify-lock-approved.mjs exits 0
    - eslint.config.mjs exists as a flat config and `pnpm lint` exits 0; .gitignore covers node_modules/ and apps/napplet/dist/
    - chromium-provisioning.json records playwright 1.59.x and a resolvable Chromium executable path; no OS-dependency install command appears anywhere
    - No vite.config.ts, no src/shell/, no map/bbox/ortho/ui feature files exist after this task
  </acceptance_criteria>
  <done>The workspace exists with the entire approved dependency set installed atomically, Chromium provisioned with evidence, hygiene files in place, and typecheck/lint/unit green — with zero application features.</done>
</task>

<task type="auto">
  <name>Task 2: Manifest-driven verification infrastructure — cases.json, ordered conditional runner, secret scanner, public-diff check</name>
  <files>apps/napplet/tests/smoke/cases.json, scripts/verify-phase-01.sh, scripts/scan-secrets.sh, package.json</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (sections "Secret Scan and Public-Diff Review", "Validation Architecture"), scripts/verify-lock-approved.mjs</read_first>
  <action>
  Create `apps/napplet/tests/smoke/cases.json` — the single source of truth for built-artifact smoke cases, initially EMPTY but schema-valid: `{ "schema_version": 1, "cases": [] }`. Case entry schema (documented in a top-of-file `$comment` field): `{ "id": "<kebab-case>", "requirement": "<REQ-ID>", "script": "<repo-relative runner file>", "args": ["--case", "<id>"], "evidence": "<repo-relative evidence path>" }`. Later plans APPEND case entries in the same task that creates the case's script — they never modify runner logic.

  Create `scripts/verify-phase-01.sh` (`set -euo pipefail`, ordered, each step echoing PASS/SKIP/FAIL with its name):
  1. unit — `pnpm --filter @terrcvm/napplet test:unit` (always).
  2. typecheck — `pnpm --filter @terrcvm/napplet typecheck` (always).
  3. lint — `pnpm lint` (always).
  4. build — `pnpm --filter @terrcvm/napplet build` ONLY IF `apps/napplet/vite.config.ts` exists (Plan 03 creates it), else SKIP with reason.
  5. verify-dist — `node scripts/verify-dist.mjs` ONLY IF that script exists (requires step 4 to have run; if vite.config.ts exists but verify-dist.mjs is missing, FAIL — build without independent dist verification is not a valid state).
  6. conformance — `pnpm --filter @terrcvm/napplet exec napplet-conformance ./dist --reporter json --out ../../.planning/evidence/phase-01/conformance.json` ONLY IF `apps/napplet/vite.config.ts` exists.
  7. manifest cases — parse `apps/napplet/tests/smoke/cases.json` with `node` (inline `node -e` or a small embedded parse): validate `schema_version` is 1 and `cases` is an array; for EVERY case, if the case's `script` file does not exist, FAIL naming the case id (a manifest case with a missing script is a broken promise, never a skip); otherwise execute `node <script> <args...>` and fail on non-zero. An empty cases array passes.
  8. shell-boundary — `node scripts/verify-shell-boundary.mjs` ONLY IF it exists (Plan 03).
  9. map-provenance — `node scripts/verify-map-provenance.mjs` (always — exists since Plan 01).
  10. source-policy — `node scripts/verify-source-policy.mjs` ONLY IF it exists (later plan).
  11. ledger — `node scripts/validate-fallback-ledger.mjs` (always).
  12. local-surface — `node scripts/verify-no-local-surface.mjs` ONLY IF it exists (later plan).
  13. lock-approved — `node scripts/verify-lock-approved.mjs` (always).
  14. secret scan — `bash scripts/scan-secrets.sh` (always).
  15. public-diff check — `git diff --check` (always; whitespace/conflict-marker hygiene for the public repo).
  The SKIP conditions exist so this runner is correct in every wave from now to phase end; each later plan turns its SKIPs into real runs by creating the backing script, and appends manifest cases, without editing this file.

  Create `scripts/scan-secrets.sh`: probe `command -v gitleaks` first, else `command -v trufflehog`; pin the found tool and exact invocation — scan tracked AND untracked files plus `apps/napplet/dist/` when it exists, redacted output, non-zero exit on findings — and record tool name/version plus a redacted summary into `.planning/evidence/phase-01/secret-scan.txt`. If NEITHER tool exists, FAIL CLOSED with a named blocker message (this starts the 30-minute OPS-01 clock; the documented fallback is operator-provided scanner installation recorded in the ledger). A hand-rolled regex may never substitute as the sole gate.

  Root `package.json`: the `verify:phase-01` / `scan:secrets` wiring from Task 1 already points here; confirm both resolve and add nothing else.

  Do NOT modify `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-VALIDATION.md` and do NOT claim wave_0 complete — build, conformance, the Paja harness, and verify-dist do not exist yet; Plan 03 completes Wave 0 honestly.
  </action>
  <verify>
    <automated>bash scripts/verify-phase-01.sh && pnpm scan:secrets && node -e "const m=require('./apps/napplet/tests/smoke/cases.json'); if(m.schema_version!==1||!Array.isArray(m.cases)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - verify-phase-01.sh passes end-to-end on the current tree with build/verify-dist/conformance/shell-boundary/source-policy/local-surface reported as SKIP (backing scripts absent) and all always-on steps PASS
    - Adding a probe case entry whose script does not exist makes the runner exit non-zero naming the case (prove once, then remove the probe)
    - scan-secrets.sh names a real established scanner and its version in .planning/evidence/phase-01/secret-scan.txt (redacted)
    - 01-VALIDATION.md is untouched; wave_0_complete remains false
  </acceptance_criteria>
  <done>The append-only verification ladder exists: a schema-valid empty manifest, a conditional ordered runner that can never silently drop a promised case, a pinned secret scanner, the lockfile-approval cross-check, and the public-diff check — all green on the featureless workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | The single Phase 1 dependency installation |
| Playwright provisioning → local machine | Browser binary download during Chromium provisioning |
| Repository → public GitHub | Lockfile, configs, and evidence are published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | Lockfile contents vs approval | high | mitigate | verify-lock-approved.mjs cross-checks pnpm ls JSON against package-audit.md on every runner pass; one mutation in history |
| T-01-06 | Information Disclosure | Secrets in repo/evidence | high | mitigate | Established scanner behind scan:secrets from this wave onward; redacted evidence; git diff --check in the runner |
| T-01-07 | Tampering | Stale/dropped verification steps in later waves | medium | mitigate | Manifest-driven runner: conditional steps activate when scripts appear; missing manifest-case scripts fail hard |
| T-01-08 | Elevation of Privilege | Unapproved OS-level install during provisioning | medium | mitigate | Only `playwright install chromium` runs; --with-deps/OS packages are explicitly outside the recorded approval |
| T-01-SC | Tampering | npm/pnpm installs | high | mitigate | Install only the Plan 01 human-approved exact pins in one reviewed lockfile mutation; public-diff review of the lockfile at wave end |
</threat_model>

<verification>
- [ ] `pnpm install --frozen-lockfile` passes (VER-02 frozen-install portion)
- [ ] `node scripts/verify-lock-approved.mjs` exits 0
- [ ] `bash scripts/verify-phase-01.sh` passes with honest SKIPs
- [ ] Wave-end secret/public-diff gate: `pnpm scan:secrets` exits 0 and `git diff --check` clean; lockfile additions match the approved pins exactly (reviewed in the public diff)
</verification>

<success_criteria>
- Entire approved Phase 1 dependency set installed in one exact-pinned lockfile mutation; Chromium provisioned with evidence; no other install ever runs in Phase 1
- .gitignore + ESLint 9 flat config exist; typecheck/lint/unit green
- Manifest-driven runner, secret scanner, lock-approval check, and public-diff check operational and append-ready for Plans 03+
</success_criteria>

<output>
Create `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-02-SUMMARY.md` when done
</output>
