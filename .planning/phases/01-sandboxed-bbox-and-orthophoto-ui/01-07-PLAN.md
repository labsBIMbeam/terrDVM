---
phase: 01-sandboxed-bbox-and-orthophoto-ui
plan: 07
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - apps/napplet/tests/smoke/paja-harness.mjs
  - apps/napplet/tests/smoke/cases.json
  - scripts/verify-no-local-surface.mjs
  - package.json
  - .planning/evidence/phase-01/local-surface-decision.json
  - .planning/evidence/phase-01/*
  - .planning/evidence/fallback-ledger.jsonl
  - tests/security/local-resource-boundary.test.mjs
  - tools/local-fallback/server.mjs
autonomous: false
requirements: [SBOX-02, SBOX-03, SBOX-04, OPS-01]
must_haves:
  truths:
    - "Four built-artifact negative cases — resource-denied, resource-timeout, preview-failed-generic, stale-late-response — pass VIA THE MANIFEST RUNNER with the exact frozen UI copy for each state, the selection and request-preview DTO remaining valid throughout every failure, and a loopback/runtime-only page request log in every case (zero direct egress)"
    - "Redacted VER-04-T1 (denied capability is actionable) and VER-04-T3 (resource timeout is actionable and a late response never renders) evidence exists under .planning/evidence/phase-01/ derived from the built artifact under real Paja — this is the Phase 1 VER-04 failure subset, produced now, not deferred to Phase 4"
    - "An honest machine-validated local-surface decision record exists: no-local-surface when the shell resource path carried every positive and negative case, or fallback-required backed by a validated >30-minute OPS-01 ledger entry naming which fallback, when, and why — never an undocumented or speculative server"
    - "The blocking human checkpoint separates the two branches: a conditional local listening surface can NEVER start before recorded human approval of its exact loopback/token/origin/source-allowlist design (approval given after the OPS-01 ledger trigger and before first run); confirming the no-local-surface branch approves nothing and starts nothing; no branch approves non-loopback exposure"
    - "In the no-local-surface branch, verify-no-local-surface.mjs proves by static scan and socket enumeration that no project listener surface exists, and root script verify:no-local-surface is wired so the manifest ladder runs it from now on; in the approved-fallback branch, RED boundary tests exist and fail BEFORE the first process start, the GREEN minimal surface sits behind the same sole adapter, and root script test:local-resource-boundary is wired and green — test:local-resource-boundary exists ONLY in the fallback branch, otherwise verify:no-local-surface is the required gate"
  artifacts:
    - path: "apps/napplet/tests/smoke/cases.json"
      provides: "Manifest extended with resource-denied, resource-timeout, preview-failed-generic, stale-late-response"
      contains: "stale-late-response"
    - path: "apps/napplet/tests/smoke/paja-harness.mjs"
      provides: "Harness extended with deterministic denied/timeout/generic-failure/late-response fault paths using the recorded Kehto/Paja mechanism"
      contains: "resource-timeout"
    - path: ".planning/evidence/phase-01/local-surface-decision.json"
      provides: "Machine-validated SBOX-02 local-surface decision: no-local-surface or fallback-required with ledger linkage"
      contains: "decision"
    - path: "scripts/verify-no-local-surface.mjs"
      provides: "Static + socket validator proving zero project listener surface (or, in the fallback branch, delegating to the boundary suite and failing if the ledger/approval record is absent)"
    - path: "package.json"
      provides: "Root script verify:no-local-surface (always) and test:local-resource-boundary (fallback branch only)"
      contains: "verify:no-local-surface"
  key_links:
    - from: "apps/napplet/tests/smoke/cases.json"
      to: "scripts/verify-phase-01.sh"
      via: "the manifest-driven runner executes the four appended negative cases automatically"
      pattern: "resource-denied"
    - from: "scripts/verify-no-local-surface.mjs"
      to: ".planning/evidence/phase-01/local-surface-decision.json"
      via: "the validator enforces the branch recorded in the decision record"
      pattern: "local-surface-decision"
    - from: "package.json"
      to: "scripts/verify-no-local-surface.mjs"
      via: "root script wiring picked up by the runner's script-exists dispatch (local-surface step turns from SKIP to PASS/FAIL)"
      pattern: "verify-no-local-surface"
    - from: "tests/security/local-resource-boundary.test.mjs"
      to: "tools/local-fallback/server.mjs"
      via: "RED boundary tests written and failing before the server's first start; GREEN implementation satisfies them (fallback branch only)"
---

<objective>
Produce the Phase 1 failure-path evidence on the built artifact — VER-04-T1 denied capability, VER-04-T3 resource timeout with stale-late-response suppression, plus the generic-failure state — with exact frozen copy, intact selection/DTO, and zero-egress request logs; then resolve the SBOX-02 local-surface question honestly: record the no-local-surface decision when the shell resource path proved sufficient, or take the documented OPS-01 fallback path through a blocking human checkpoint that must approve the exact loopback/token/origin/source-limited design BEFORE any listener first starts. Task 3 executes whichever branch the checkpoint confirmed, fail closed.

Purpose: the roadmap requires the VER-04 Phase 1 subset and the early SBOX-05 boundary as exit gates. Negative paths are per-phase evidence, never a terminal batch; and a local listening surface may never appear "temporarily unauthenticated" — it is either provably absent or born behind auth with recorded approval.
Output: four manifest negative cases with redacted evidence, the local-surface decision record, verify-no-local-surface validator wired as a root script, and — only in the approved-fallback branch — RED-first boundary tests plus the minimal token-gated loopback surface behind the same sole adapter. Zero installs; zero lockfile mutations.
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
@$HOME/.hermes/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-06-SUMMARY.md
@apps/napplet/tests/smoke/paja-harness.mjs
@apps/napplet/src/ui/copy.ts
@.planning/evidence/phase-01/kehto-cli-contract.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Built-artifact negative cases (denied, timeout, generic-failed, stale-late-response) with VER-04-T1/T3 evidence, plus the local-surface decision record</name>
  <files>apps/napplet/tests/smoke/paja-harness.mjs, apps/napplet/tests/smoke/cases.json, .planning/evidence/phase-01/local-surface-decision.json, .planning/evidence/phase-01/paja-resource-denied.json, .planning/evidence/phase-01/paja-resource-timeout.json, .planning/evidence/phase-01/paja-preview-failed-generic.json, .planning/evidence/phase-01/paja-stale-late-response.json, .planning/evidence/fallback-ledger.jsonl</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md (Copywriting Contract denied/timeout/failed rows, Preview machine State→copy binding), .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (Assumption A5, "Actual Paja/Kehto Smoke Protocol" steps 6-7), .planning/evidence/phase-01/kehto-cli-contract.json, apps/napplet/src/ui/copy.ts</read_first>
  <precondition>Plan 06 complete with preview-ready-live green via the manifest runner (verify-source-policy --require-live exits 0); kehto-cli-contract.json exists with a non-blocked status. No install runs in this plan.</precondition>
  <action>
  Extend `apps/napplet/tests/smoke/paja-harness.mjs` with four negative cases against the BUILT artifact under real Kehto/Paja. Fault mechanisms: for `resource-denied`, relaunch with the resource capability off using the exact capability mechanism recorded in kehto-cli-contract.json (same mechanism as boot-denied). For timeout/generic-failure/late-response, prefer the deterministic resource fault/delay injection mechanism recorded in kehto-cli-contract.json if the probe captured one; if Paja offers no injection point, use a harness-process-owned loopback interposer between Paja's resource service and upstream — created and destroyed inside the harness process lifetime, loopback-only, never committed as a server, never reachable outside the test run, and explicitly NOT the SBOX-05 fallback surface (record in each case's evidence JSON which mechanism was used). Each case first draws a valid bbox so failure states are exercised against a real selection.

  The four cases, each asserting the rendered DOM against the frozen COPY strings (assert the visible text content; bold markup is presentation):
  - `resource-denied` (VER-04-T1): capability off; the preview pane renders exactly the "Resource denied" contract string ("Imagery access was denied by the shell. Your selection and request preview remain valid. Grant the resource capability in the shell, then choose Retry preview.") with a focusable 44px Retry preview button; no unhandled rejection; the bbox rectangle, area readout, and request-preview DTO remain populated and unchanged.
  - `resource-timeout` (VER-04-T3, first half): injected delay exceeds the TIMEOUT_S=15 client deadline; the pane renders exactly the timeout contract string with "15" interpolated; selection/DTO unchanged; Retry preview present.
  - `preview-failed-generic`: injected non-timeout, non-denial failure (e.g. malformed/error response); the pane renders ONLY the generic "Preview failed" contract string — the case fails if the denied or timeout wording appears; selection/DTO unchanged.
  - `stale-late-response` (VER-04-T3, second half): start a load for bbox A with an injected long delay, draw bbox B, let B's preview settle, then release A's delayed response; assert A's payload NEVER renders (pane state and DTO source row still correspond to B) and the aria-live region announced no stale update.

  EVERY case asserts the page-level request log contains loopback/runtime-allowlisted origins only — zero direct external egress (the SBOX-03 dynamic contract holds under failure injection too). Write per-case redacted JSON + screenshot evidence to `.planning/evidence/phase-01/paja-<case>.json`; the resource-denied and resource-timeout/stale evidence files each carry a `ver04` field naming VER-04-T1 or VER-04-T3 so the Plan 09 gate can map the subset mechanically.

  APPEND the four entries to `apps/napplet/tests/smoke/cases.json` (existing entry shape; requirements SBOX-02 for denied — its evidence doubles as VER-04-T1 — and MAP-05/SBOX-02 for the others) in this same task; the manifest-driven runner executes them automatically with no runner edit.

  Then write `.planning/evidence/phase-01/local-surface-decision.json`, derived from evidence, not preference: `{ "schema_version": 1, "requirement": "SBOX-02", "decided_at": "<RFC3339 UTC>", "decision": "no-local-surface" | "fallback-required", "basis": [...], "ledger_ref": null | "<blocker_id>" }`. Record `no-local-surface` only if every positive and negative smoke case ran through the feature-detected shell resource path and no >30-minute SBOX-02 blocker occurred anywhere in Phase 1 (check kehto-cli-contract.json status and the fallback ledger). Record `fallback-required` only if a real SBOX-02 integration blocker exceeded 30 minutes: in that case FIRST append the honest OPS-01 entry to `.planning/evidence/fallback-ledger.jsonl` (which fallback, when, why; validate with `node scripts/validate-fallback-ledger.mjs`) and set `ledger_ref` to its blocker_id. Do NOT design, scaffold, or start any server in this task under either decision — the fallback design is presented at the Task 2 checkpoint and built only in Task 3 after approval.
  </action>
  <verify>
    <automated>bash scripts/verify-phase-01.sh && node -e "const m=require('./apps/napplet/tests/smoke/cases.json'); for(const id of ['resource-denied','resource-timeout','preview-failed-generic','stale-late-response']) if(!m.cases.some(c=>c.id===id)) process.exit(1)" && node -e "const d=require('./.planning/evidence/phase-01/local-surface-decision.json'); if(!['no-local-surface','fallback-required'].includes(d.decision)) process.exit(1); if(d.decision==='fallback-required'&&!d.ledger_ref) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - All four negative cases pass via the manifest runner; each evidence JSON records the exact rendered copy, the intact selection/DTO assertion, the fault mechanism used, and a loopback-only request log
    - resource-denied evidence carries ver04 VER-04-T1; resource-timeout and stale-late-response evidence carry ver04 VER-04-T3; all evidence redacted
    - local-surface-decision.json is consistent with the ledger: no-local-surface has ledger_ref null and an empty (or unrelated-only) ledger; fallback-required has a validated ledger entry it references
    - No server, listener, or fallback scaffolding appears in this task's diff
  </acceptance_criteria>
  <done>The Phase 1 VER-04 failure subset exists as built-artifact manifest evidence with exact truthful copy and zero egress, and the SBOX-02 local-surface question has one honest machine-checkable answer awaiting the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING local-surface checkpoint — confirm no-local-surface, or approve the exact fallback boundary design before any listener ever starts</name>
  <what-built>Task 1 produced the four built-artifact negative cases (VER-04-T1/T3 evidence included) and `.planning/evidence/phase-01/local-surface-decision.json`. If the decision is `fallback-required`, a validated OPS-01 ledger entry records the >30-minute SBOX-02 blocker, and the executor has prepared (on paper, in the checkpoint presentation — no code, no process) the exact fallback design: bind address 127.0.0.1 (and ::1 if IPv6), a startup-generated scoped token required on every endpoint and never committed or embedded in browser assets, the exact allowed Paja/Napplet origin and capability policy, the exact source/URL allowlist mirroring source-policy.json, bounded response bytes/time/redirects/methods/content types, and startup refusal on missing token or non-loopback bind. NOTHING has been started: the conditional local listening surface always requires this recorded human approval after the OPS-01 ledger trigger and before its first run.</what-built>
  <how-to-verify>
    1. Open `.planning/evidence/phase-01/local-surface-decision.json` and the fallback ledger. Confirm the decision matches reality: every smoke case (including the four negative cases) ran through the shell resource path → no-local-surface; a real recorded >30-minute SBOX-02 blocker → fallback-required with its ledger entry (which fallback, when, why) already validated.
    2. If the decision is no-local-surface: confirm no ledger entry claims a fallback and no server code exists anywhere in the diff (`git log --stat` for Plans 03-07). This branch approves nothing and starts nothing — your confirmation only records which branch Task 3 executes.
    3. If the decision is fallback-required: review the presented design against ALL of — loopback-only bind; startup-generated scoped token on every endpoint (including health/read routes); exact origin/capability allowlist (never "*"); source allowlist limited to the approved source-policy roles; bounded bytes/time/redirects; token provisioned at runtime only, never committed, never in dist or browser storage; fail closed on missing/wrong auth and on any non-loopback bind. Approval covers exactly this design and loopback exposure ONLY — no branch of this checkpoint can approve non-loopback exposure; that would require its own separately recorded approval which Phase 1 never grants.
  </how-to-verify>
  <resume-signal>Type "no-local-surface-confirmed" (decision record says no-local-surface; Task 3 runs only the no-surface validator) or "approved-fallback" (decision record says fallback-required; authorizes exactly the presented loopback/token/origin/source-limited design, RED tests before first start) — or describe the mismatch if the decision record does not match the evidence.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute the confirmed branch — no-local-surface validator wired, OR RED-first boundary tests then the minimal approved token-gated loopback surface</name>
  <files>scripts/verify-no-local-surface.mjs, package.json, apps/napplet/tests/smoke/cases.json, tests/security/local-resource-boundary.test.mjs, tools/local-fallback/server.mjs, .planning/evidence/phase-01/local-surface-decision.json</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (Pattern 6 "Conditional Local Fallback Is Secure From Birth"), .planning/evidence/phase-01/local-surface-decision.json, scripts/verify-phase-01.sh, apps/napplet/src/shell/resource-client.ts</read_first>
  <precondition>Task 2 resumed with "no-local-surface-confirmed" or "approved-fallback"; the resume value matches local-surface-decision.json. Halt on any mismatch.</precondition>
  <behavior>
    Fallback branch only (RED before the first process start — these tests exist and FAIL before `tools/local-fallback/server.mjs` is ever executed):
    - Test: startup with no token configured refuses to start (fail closed).
    - Test: startup requested on a non-loopback bind (e.g. 0.0.0.0) refuses to start; only 127.0.0.1/::1 are accepted, and only because this checkpoint-approved run authorizes loopback.
    - Test: every endpoint (retrieval, health, any state route) returns an auth failure for a missing token and for a wrong token; no route is exempt.
    - Test: a request with a disallowed Origin (anything outside the exact approved Paja/Napplet origin list) is rejected; "*" never appears in any CORS/capability response.
    - Test: a source/URL outside the approved source-policy allowlist is rejected before any upstream request; response bytes over the bound, upstream time over the bound, and any redirect are each rejected.
    - Test: a static scan finds the token in no committed file and no dist output; the token arrives only via runtime environment/local file outside the repository.
  </behavior>
  <action>
  BOTH branches: create `scripts/verify-no-local-surface.mjs` (plain Node) and wire root script `"verify:no-local-surface": "node scripts/verify-no-local-surface.mjs"` in `package.json` — the Plan 02 runner's local-surface step turns from SKIP to a real gate from this commit onward. The validator reads `local-surface-decision.json` first and enforces the recorded branch, fail closed on any inconsistency.

  NO-LOCAL-SURFACE branch (resume was no-local-surface-confirmed): implement the validator as (a) static scan — no file under apps/, scripts/, tools/, or tests/ creates a listening socket (search for server/listen primitives, comment-filtered so prose cannot self-trigger); the only allowlisted occurrences are the Vite preview launcher and deterministic harness-lifetime fault interposer, both path-scoped to `apps/napplet/tests/smoke/`, plus Vite internals under node_modules — no production/source/tool path is allowlisted; (b) decision consistency — decision is no-local-surface, ledger_ref null, and `tools/local-fallback/` plus `tests/security/` do not exist; (c) socket enumeration — during a runner pass, no process bound by the repository listens on any non-loopback address, and no repository-owned listener exists at all outside those path-scoped harness-lifetime preview/interposer ports (enumerate via `node:net` probe of the documented harness ports plus a check that no leftover listener from the suite persists after the run). Do NOT create tests/security/ or tools/local-fallback/ in this branch; `test:local-resource-boundary` is NOT wired — verify:no-local-surface is the required gate. Update local-surface-decision.json with `"confirmed_by_checkpoint": true` and the resume value.

  APPROVED-FALLBACK branch (resume was approved-fallback): TDD strictly — write `tests/security/local-resource-boundary.test.mjs` implementing every behavior above; run it to confirm RED with the server file absent; commit (`test(01-07): failing local-resource boundary tests before first start`). Only then implement `tools/local-fallback/server.mjs` as the MINIMAL surface satisfying the tests: loopback-only bind, startup-generated scoped token checked on every endpoint, exact origin/capability allowlist, source allowlist limited to the approved source-policy roles, bounded bytes/time/redirects, fail-closed startup. The built Napplet reaches it EXCLUSIVELY through the existing sole shell adapter (the UI gains no second transport code path — resource-client.ts remains the only transport seam and the LOCAL FALLBACK badge/copy paths activate per the UI-SPEC transport-3 contract). Wire root script `"test:local-resource-boundary": "node --test tests/security/local-resource-boundary.test.mjs"` — this script exists ONLY in this branch — and in this branch verify-no-local-surface.mjs delegates: it passes only when the decision is fallback-required, the referenced ledger entry validates, the boundary suite is green, and a token scan of committed files and dist finds nothing. Append a manifest case running the boundary suite so the ladder carries it. Commit GREEN separately.

  Both branches end by running the full ladder (`bash scripts/verify-phase-01.sh`) and confirming the local-surface step now executes as a hard gate.
  </action>
  <verify>
    <automated>node scripts/verify-no-local-surface.mjs && bash scripts/verify-phase-01.sh && node -e "const p=require('./package.json'); if(!p.scripts['verify:no-local-surface']) process.exit(1); const d=require('./.planning/evidence/phase-01/local-surface-decision.json'); const fb=d.decision==='fallback-required'; if(fb!==!!p.scripts['test:local-resource-boundary']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - verify:no-local-surface is wired and the runner's local-surface step executes it (no SKIP remains)
    - No-local-surface branch: validator green, no tests/security/ or tools/local-fallback/ paths exist, no boundary script wired
    - Fallback branch: RED commit precedes the server's first start; every boundary behavior passes; the server binds loopback only with the token on every endpoint; token absent from all committed files and dist; the manifest carries the boundary case
    - The executed branch matches both the checkpoint resume value and the decision record; any mismatch halted the task
  </acceptance_criteria>
  <done>The early SBOX-05 boundary is satisfied fail-closed: either the absence of any local surface is machine-proven and permanently gated in the ladder, or the approved fallback surface exists born-secure behind RED-first boundary tests, the same sole adapter, and its recorded OPS-01 trigger and human approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fault injection → built artifact | Harness-induced failures must not open alternative transports or mask real behavior |
| Conditional local surface → local machine | A listener, if it exists, is reachable by other local processes and browser contexts |
| Decision record → phase gate | A dishonest decision would launder an unauthenticated surface or a fake fallback |
| Evidence → public GitHub | Negative-case logs and screenshots are published redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-36 | Spoofing | Local process/browser context using an unauthenticated local surface | high | mitigate | No listener without checkpoint approval; loopback bind + startup token on every endpoint; RED boundary tests before first start; verify-no-local-surface as a hard ladder step |
| T-01-37 | Tampering | Stale/late response rendering for a newer selection | high | mitigate | stale-late-response built-browser case on top of Plan 06 unit correlation; VER-04-T3 evidence |
| T-01-38 | Information Disclosure | Fallback token in committed files, dist, or evidence | high | mitigate | Runtime-only token provisioning, token scan in the validator, scan:secrets over all new evidence |
| T-01-39 | Repudiation | Fallback taken without an OPS-01 record, or no-surface claimed despite a blocker | medium | mitigate | Decision record cross-validated against the ledger and kehto-cli-contract status; checkpoint verifies the match |
| T-01-40 | Denial of Service | Failure states breaking selection/DTO or hiding retry | medium | mitigate | Every negative case asserts intact selection/DTO and the Retry preview action with exact frozen copy |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero installs in this plan; lock-approved ladder step; public-diff review at wave end |
</threat_model>

<verification>
- [ ] `bash scripts/verify-phase-01.sh` green end-to-end with the four negative cases executed from the manifest and the local-surface step a hard gate
- [ ] VER-04-T1 and VER-04-T3 evidence files exist, redacted, with ver04 markers and loopback-only request logs
- [ ] Checkpoint outcome recorded as no-local-surface-confirmed or approved-fallback, matching the decision record; no listener started before approval in the fallback branch
- [ ] Wave-end secret/public-diff gate: `pnpm scan:secrets` exits 0 over repo + dist + new evidence; `git diff --check` clean; no lockfile delta in this plan's commits
</verification>

<success_criteria>
- The Phase 1 failure subset (VER-04-T1/T3 plus generic-failed and stale suppression) is proven on the built artifact with exact truthful copy, intact selection/DTO, and zero direct egress
- The SBOX-02/early-SBOX-05 question is closed fail-closed with a machine-validated decision, a blocking human gate, and a permanently wired ladder step for whichever branch is real
- All direct egress remains prohibited and request-log-asserted in every new case
</success_criteria>

<output>
Create `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-07-SUMMARY.md` when done
</output>
