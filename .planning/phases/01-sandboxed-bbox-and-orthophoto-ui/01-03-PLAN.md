---
phase: 01-sandboxed-bbox-and-orthophoto-ui
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/napplet/vite.config.ts
  - apps/napplet/index.html
  - apps/napplet/src/main.ts
  - apps/napplet/src/shell/resource-client.ts
  - apps/napplet/src/config/defaults.ts
  - apps/napplet/src/ui/tokens.css
  - apps/napplet/src/ui/app.ts
  - apps/napplet/tests/unit/shell-adapter.test.ts
  - apps/napplet/tests/unit/defaults.test.ts
  - scripts/verify-dist.mjs
  - apps/napplet/tests/smoke/paja-harness.mjs
  - apps/napplet/tests/smoke/cases.json
  - scripts/verify-shell-boundary.mjs
  - .planning/evidence/phase-01/*
  - .planning/evidence/fallback-ledger.jsonl
autonomous: true
requirements: [SBOX-01, SBOX-02, SBOX-03]
must_haves:
  truths:
    - "A production single-file dist/index.html builds via the native Napplet plugin, passes the independent verify-dist inventory, and passes napplet-conformance against dist including the no-capability degraded pass"
    - "The built artifact boots under the actual Kehto/Paja sandbox (never the Vite dev server) rendering the exact truthful empty/no-source state from the UI-SPEC Copywriting Contract"
    - "Exactly one feature-detected adapter owns window.napplet and @napplet/sdk; the static boundary scan passes, and the Paja request log proves zero direct network egress outside the adapter path and the harness-allowlisted loopback preview origin"
    - "The adapter's denied and timeout behaviors were written as failing tests BEFORE implementation, and src/config/defaults.ts mirrors the approved v1-defaults.json values exactly"
    - "The Kehto/Paja CLI contract was probed from `kehto paja --help` and recorded before the harness relied on any flag; if the observed contract is incompatible or blocked over 30 minutes, an OPS-01 ledger entry is appended and the plan stops at the documented SBOX-02 fallback contract — no server is created here"
    - "The boot cases live in cases.json and are executed by the manifest-driven runner, not only by direct invocation"
  artifacts:
    - path: "apps/napplet/src/shell/resource-client.ts"
      provides: "Sole feature-detected shell resource adapter with normalized CAPABILITY_DENIED/UNAVAILABLE/TIMEOUT/FAILED errors"
      contains: "window.napplet"
    - path: "apps/napplet/src/config/defaults.ts"
      provides: "Fixed v1 defaults consumed app-wide, mirroring v1-defaults.json (RES_M 5, OUTPUT_MIME model/gltf-binary, MAX_AREA_KM2 100, TIMEOUT_S 15)"
      contains: "model/gltf-binary"
    - path: "apps/napplet/vite.config.ts"
      provides: "Native Napplet single-file production build"
      contains: "single-file"
    - path: "scripts/verify-dist.mjs"
      provides: "Independent dist inventory assertions (single file, no remote executable assets, no secret-like patterns)"
    - path: "apps/napplet/tests/smoke/paja-harness.mjs"
      provides: "Built-artifact Paja/Chromium harness importing the DIRECT playwright dependency, with per-case JSON evidence and request-log capture"
      contains: "playwright"
    - path: "apps/napplet/tests/smoke/cases.json"
      provides: "Manifest now containing the boot and boot-denied cases"
      contains: "boot"
    - path: "scripts/verify-shell-boundary.mjs"
      provides: "Static scan proving privileged globals are reached only inside src/shell/"
    - path: ".planning/evidence/phase-01/kehto-cli-contract.json"
      provides: "Observed kehto/paja CLI flags, versions, and package provenance recorded before harness use"
  key_links:
    - from: "apps/napplet/vite.config.ts"
      to: "apps/napplet/dist/index.html"
      via: "nip5aManifest({ artifactMode: 'single-file' }) from @napplet/vite-plugin"
      pattern: "single-file"
    - from: "apps/napplet/src/ui/app.ts"
      to: "apps/napplet/src/shell/resource-client.ts"
      via: "UI consumes capability state only through the adapter"
    - from: "apps/napplet/src/config/defaults.ts"
      to: ".planning/evidence/phase-01/v1-defaults.json"
      via: "defaults.test.ts reads both and asserts exact equality of the four pinned values"
      pattern: "v1-defaults"
    - from: "apps/napplet/tests/smoke/paja-harness.mjs"
      to: ".planning/evidence/phase-01/kehto-cli-contract.json"
      via: "harness launches kehto only with flags recorded by the probe"
      pattern: "kehto-cli-contract"
    - from: "apps/napplet/tests/smoke/cases.json"
      to: "scripts/verify-phase-01.sh"
      via: "runner executes the appended boot cases from the manifest"
---

<objective>
Prove the entire Phase 1 architecture end-to-end as the Walking Skeleton: TDD the sole shell resource adapter (denied/timeout RED first), consume the pinned v1 defaults, render the minimal truthful semantic UI from tokens, produce the native single-file production build with independent dist verification, then verify the built artifact under real Kehto/Paja — CLI contract probed and recorded first — with conformance, boot and no-capability cases, and a request-log assertion of zero direct network egress. Finish by running the full manifest-driven ladder and writing redacted evidence of the honest empty/no-source state.

Purpose: dev-server success is explicitly not evidence (SBOX-01/02). Proving single-file build + conformance + opaque-origin boot + the adapter seam + the probed sandbox harness now catches the highest-risk assumptions on the skeleton, before any map/ortho feature exists.
Output: adapter + defaults + tokens + truthful boot UI, vite single-file config, verify-dist, probed CLI contract record, Paja harness with boot/boot-denied manifest cases, shell-boundary scan, conformance/Paja evidence, and a green ladder. Zero lockfile mutations — everything was installed in Plan 02.
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-02-SUMMARY.md
@.planning/evidence/phase-01/v1-defaults.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TDD shell adapter + pinned defaults + minimal truthful UI + native single-file build with independent verify-dist</name>
  <files>apps/napplet/src/shell/resource-client.ts, apps/napplet/src/config/defaults.ts, apps/napplet/src/ui/tokens.css, apps/napplet/src/ui/app.ts, apps/napplet/src/main.ts, apps/napplet/index.html, apps/napplet/vite.config.ts, apps/napplet/tests/unit/shell-adapter.test.ts, apps/napplet/tests/unit/defaults.test.ts, scripts/verify-dist.mjs</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (Pattern 2 "One Feature-Detected Shell Adapter", Pattern 4 "Single-File Is a Build Invariant", "Production Artifact Assertions"), .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md (Design Tokens, Copywriting Contract, Component Inventory row 1), .planning/evidence/phase-01/v1-defaults.json</read_first>
  <precondition>Plan 02 complete: pnpm-lock.yaml contains the full approved pin set (node scripts/verify-lock-approved.mjs exits 0) and Chromium provisioning evidence exists. No install runs in this plan.</precondition>
  <behavior>
    - Test 1 (`resource_capability_absence_maps_to_actionable_denied_state`, named case 15 from 01-RESEARCH.md): when `window.napplet?.resource` is undefined, the adapter yields a normalized `PreviewError` with code `CAPABILITY_DENIED` — never an unhandled TypeError.
    - Test 2 (timeout groundwork for named case 16): a request exceeding the client deadline (`TIMEOUT_S` from config → milliseconds) resolves to code `TIMEOUT` via AbortController against a fake slow shell stub; a distinct non-timeout stub failure maps to `FAILED`.
    - Test 3 (defaults.test.ts): `src/config/defaults.ts` exports exactly RES_M=5, OUTPUT_MIME="model/gltf-binary", MAX_AREA_KM2=100, TIMEOUT_S=15, and the test reads `.planning/evidence/phase-01/v1-defaults.json` from disk and asserts equality field-by-field — drift between code and the approved decision artifact fails the suite.
  </behavior>
  <action>
  RED first: write `apps/napplet/tests/unit/shell-adapter.test.ts` and `apps/napplet/tests/unit/defaults.test.ts` against not-yet-existing modules; run `pnpm --filter @terrcvm/napplet test:unit` to confirm failure; commit (`test(01-03): add failing shell-adapter and defaults tests`). Remove `passWithNoTests` from vitest.config.ts in the same commit — real tests now exist. Then GREEN:

  `src/shell/resource-client.ts` (the permanent SBOX-03 seam): exports `getResourceCapability()` feature-detecting `window.napplet?.resource`, and `loadApprovedBytes(url, { signal, deadlineMs })` enforcing a caller-supplied allowlist-check hook and a client-side deadline via AbortController, normalizing all failures to `PreviewError` with code `CAPABILITY_DENIED` | `UNAVAILABLE` | `TIMEOUT` | `FAILED`. No other file may touch `window.napplet` or import `@napplet/sdk` — later plans plug map tiles and ortho preview into this seam; it is the ONLY transport.

  `src/config/defaults.ts`: export the four pinned values exactly as approved in v1-defaults.json — `RES_M = 5`, `OUTPUT_MIME = 'model/gltf-binary'`, `MAX_AREA_KM2 = 100`, `TIMEOUT_S = 15`. These are the recorded MAP-07 planning decision; do not choose different values, do not scatter literals into components.

  `src/ui/tokens.css`: the `:root` design-token block copied EXACTLY from 01-UI-SPEC.md "Design Tokens" (colors, fonts, sizes, weights, spacing, --target-min, --focus-ring, --radius, --panel-width, motion). No other color/size/weight/spacing/radius value may appear in any Phase 1 CSS.

  `src/ui/app.ts` + `index.html` + `main.ts`: minimal semantic workbench shell — landmark structure (`header` toolbar placeholder, `main` map region placeholder, `aside` request panel), skip link "Skip to request panel" as first focusable, dark theme from tokens only. Render the truthful boot state with EXACT UI-SPEC Copywriting Contract strings: empty-state heading "No area selected" plus its body string; a "Request preview" panel with the bbox row label "Bounding box (W, S, E, N — EPSG:4326)" and em-dash value placeholders, CRS EPSG:4326, fixed-default rows "Resolution: 5 m/px — fixed for v1" and "Output: model/gltf-binary — fixed for v1" rendered from `defaults.ts` (never hardcoded strings) with the helper caption, and the source row "Source: — unavailable"; plus the "Source unavailable" state copy since no trusted source is configured. No map, no ortho, no bbox interaction, no fake imagery, no accent source dot (accent is live-only per UI-SPEC).

  `vite.config.ts`: the Napplet plugin with `artifactMode: 'single-file'` — `dist/index.html` is the sole production payload; no second single-file plugin.

  `scripts/verify-dist.mjs` (plain Node, independent of the build plugin): fails unless `apps/napplet/dist/index.html` exists non-empty and self-contained; no local JS/CSS sidecar remains a required runtime dependency; no remote executable script/stylesheet/font/worker or unaudited remote image reference; no source map unless explicitly approved; no credential/token/authenticated-URL pattern. Filter comment lines before counting (`grep -v '^#'`-style) per gate hygiene.

  Build and verify: `pnpm --filter @terrcvm/napplet build` then `node scripts/verify-dist.mjs`. Commit GREEN (`feat(01-03): shell adapter, pinned defaults, truthful boot UI, single-file build`), refactor with tests green if needed.
  </action>
  <verify>
    <automated>pnpm --filter @terrcvm/napplet test:unit && pnpm --filter @terrcvm/napplet typecheck && pnpm lint && pnpm --filter @terrcvm/napplet build && node scripts/verify-dist.mjs</automated>
  </verify>
  <acceptance_criteria>
    - RED commit precedes GREEN commit; shell-adapter and defaults tests pass with the named behaviors
    - defaults.test.ts proves defaults.ts equals v1-defaults.json field-by-field
    - vite.config.ts contains "artifactMode: 'single-file'"; verify-dist.mjs exits 0 on the fresh build
    - tokens.css matches the UI-SPEC token block verbatim; rendered copy matches the Copywriting Contract strings exactly
    - pnpm-lock.yaml is unchanged by this task (git diff shows no lockfile delta)
  </acceptance_criteria>
  <done>The single happy path — pinned defaults → adapter seam → tokens/semantic UI → single-file dist → independent dist verification — works end-to-end and is committed. Every later plan expands from this skeleton without adding transports or defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Probe the Kehto/Paja CLI contract, then build the Playwright harness with conformance, boot cases, and the no-egress request-log assertion</name>
  <files>.planning/evidence/phase-01/kehto-cli-contract.json, apps/napplet/tests/smoke/paja-harness.mjs, apps/napplet/tests/smoke/cases.json, scripts/verify-shell-boundary.mjs, .planning/evidence/phase-01/conformance.json, .planning/evidence/phase-01/paja-boot.json, .planning/evidence/phase-01/paja-boot-denied.json</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (sections "Actual Paja/Kehto Smoke Protocol", "Environment Availability"), .planning/evidence/phase-01/chromium-provisioning.json, apps/napplet/tests/smoke/cases.json</read_first>
  <precondition>kehto CLI is on PATH or at the locally observed location (research trace: /home/flx/.deno/bin/kehto with @kehto/paja@0.8.0 in the Deno cache); Chromium provisioning evidence from Plan 02 exists.</precondition>
  <action>
  PROBE FIRST — never assume CLI flags: run `command -v kehto`, `kehto --help`, and `kehto paja --help`; record into `.planning/evidence/phase-01/kehto-cli-contract.json` the executable path, observed subcommands/flags verbatim (redacted per evidence policy), the local package provenance (@kehto/cli@0.2.16 executable, @kehto/paja@0.8.0 Deno-cache path), the probe timestamp, and the exact launch command line the harness will use — constructed ONLY from observed flags (the research-expected shape is `kehto paja --target-url http://127.0.0.1:4173 -- <preview command>`, but the recorded observation wins). If the observed contract does not support launching against a local preview URL, or probing/executing blocks for more than 30 minutes, this is the documented SBOX-02/OPS-01 path: append a validated entry to `.planning/evidence/fallback-ledger.jsonl` (which fallback, when, why), record status "blocked" in kehto-cli-contract.json, and STOP this task after Task 3's ladder run — the fallback resource surface is owned by a later plan behind a blocking human checkpoint; do NOT create any server or workaround transport here.

  Create `apps/napplet/tests/smoke/paja-harness.mjs`: a Node script importing `playwright` DIRECTLY (the approved direct devDependency of apps/napplet — resolution walks up from the harness file location; NEVER import via @napplet/conformance-cli's node_modules). The harness (1) asserts `apps/napplet/dist/index.html` exists and refuses to run against a dev server — built-artifact acceptance only; (2) starts `vite preview --host 127.0.0.1 --port 4173` and launches kehto with the exact recorded command line from kehto-cli-contract.json; (3) drives Chromium against the Paja runtime, capturing console messages, page errors, and a COMPLETE request log (every request URL via Playwright request events); (4) supports `--case <name>` selection writing per-case redacted JSON evidence to `.planning/evidence/phase-01/paja-<case>.json`.

  Implement two cases: `boot` — the skeleton loads under Paja; skip link is first focusable; empty-state heading text equals "No area selected"; the request panel shows "Resolution: 5 m/px — fixed for v1", "Output: model/gltf-binary — fixed for v1", and "Source: — unavailable"; zero uncaught exceptions. `boot-denied` — relaunch with the resource capability off (per the recorded CLI contract's capability mechanism); identical assertions — the skeleton needs no capability.

  REQUEST-LOG EGRESS ASSERTION (both cases): every logged request URL must target the harness-allowlisted loopback origins only (the 127.0.0.1:4173 preview origin and the observed Paja runtime origin). Since no source is configured and the adapter is the sole transport, ANY other request — external host, non-loopback IP, data-exfil pattern, MapLibre-style tile/glyph fetch — fails the case. This is the dynamic half of SBOX-03: the built artifact makes zero direct network egress outside the sole adapter path.

  Create `scripts/verify-shell-boundary.mjs` (static half of SBOX-03): scan `apps/napplet/src/**` and fail if `window.napplet` or an `@napplet/sdk` import appears outside `src/shell/`; filter comment lines before matching so prose cannot self-invalidate the gate.

  APPEND to `apps/napplet/tests/smoke/cases.json` (do not restructure it): entries `{ "id": "boot", "requirement": "SBOX-02", "script": "apps/napplet/tests/smoke/paja-harness.mjs", "args": ["--case","boot"], "evidence": ".planning/evidence/phase-01/paja-boot.json" }` and the boot-denied equivalent — the Plan 02 runner now executes both automatically.

  Run conformance against the built dist: `pnpm --filter @terrcvm/napplet exec napplet-conformance ./dist --reporter json --out ../../.planning/evidence/phase-01/conformance.json` — the degraded (no-capability) pass must remain enabled and green.
  </action>
  <verify>
    <automated>node -e "const c=require('./.planning/evidence/phase-01/kehto-cli-contract.json'); if(!c.observed_help||!c.launch_command) process.exit(1)" && node scripts/verify-shell-boundary.mjs && pnpm --filter @terrcvm/napplet exec napplet-conformance ./dist --reporter json --out ../../.planning/evidence/phase-01/conformance.json && node apps/napplet/tests/smoke/paja-harness.mjs --case boot && node apps/napplet/tests/smoke/paja-harness.mjs --case boot-denied</automated>
  </verify>
  <acceptance_criteria>
    - kehto-cli-contract.json records observed (not assumed) CLI help output and the exact launch command before any harness run
    - paja-harness.mjs imports playwright directly; grep shows no import path through @napplet/conformance-cli
    - paja-boot.json and paja-boot-denied.json record passing assertions including the loopback-only request log with zero external requests
    - conformance.json reports exit 0 including the degraded pass; cases.json contains exactly the two new boot entries
    - verify-shell-boundary.mjs exits 0 and demonstrably fails on a probe reference to window.napplet outside src/shell/ (prove once, remove probe)
  </acceptance_criteria>
  <done>SBOX-01 (single-file + conformance) and SBOX-02 (built artifact under real Paja) are proven on the skeleton with a recorded CLI contract, and SBOX-03 is enforced both statically and dynamically via the no-egress request log. The manifest now carries the boot cases for every future ladder run.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Walking Skeleton ladder end-to-end and record redacted evidence of the truthful empty/no-source state</name>
  <files>.planning/evidence/phase-01/skeleton-ladder.log, .planning/evidence/fallback-ledger.jsonl</files>
  <read_first>scripts/verify-phase-01.sh, apps/napplet/tests/smoke/cases.json, .planning/evidence/phase-01/kehto-cli-contract.json</read_first>
  <precondition>Tasks 1 and 2 committed; if kehto-cli-contract.json records status "blocked", the ladder still runs (boot cases will fail or be blocked) and this task records the honest blocked outcome instead of green.</precondition>
  <action>
  Run the full manifest-driven ladder exactly as later waves will: `bash scripts/verify-phase-01.sh 2>&1 | tee .planning/evidence/phase-01/skeleton-ladder.log` (redact the log per evidence policy before commit). This must show: unit, typecheck, lint, build, verify-dist, conformance, BOTH manifest boot cases executed BY THE RUNNER (not only by direct invocation), shell-boundary, map-provenance, ledger, lock-approved, secret scan, and public-diff check all PASS, with source-policy and local-surface as the only remaining SKIPs (their scripts belong to later plans). If any step's outcome differs, record it truthfully — never edit the log.

  Confirm and record in the ladder log commentary (or a trailing JSON line) the exact truthful UI state the skeleton ships: no bbox tools, no map, no imagery; empty-state copy "No area selected"; fixed-default rows sourced from defaults.ts; source row "— unavailable". This is the honest zero-feature baseline the phase's remaining plans expand — no capture may imply map or preview functionality exists yet.

  OPS-01 branch (only if triggered): if any Paja/Kehto integration blocker in this plan exceeded 30 minutes, verify the Task 2 ledger entry exists and validates (`node scripts/validate-fallback-ledger.mjs`), ensure the boot-case evidence records the blocked state honestly, and STOP — the documented SBOX-02 fallback (a local authenticated resource surface under the early SBOX-05 boundary) is created only by a later plan behind a blocking human checkpoint, never here, never unauthenticated, never as a harness workaround. Wave_0/VALIDATION bookkeeping is untouched in this pass (plan-checker instruction); the SUMMARY records actual state.
  </action>
  <verify>
    <automated>bash scripts/verify-phase-01.sh && node scripts/validate-fallback-ledger.mjs && test -s .planning/evidence/phase-01/skeleton-ladder.log</automated>
  </verify>
  <acceptance_criteria>
    - skeleton-ladder.log records the full runner pass with boot cases executed via the manifest and only source-policy/local-surface skipped
    - The ledger is either still empty (no blocker occurred) or contains a validated entry for each >30-minute blocker; no fake entries
    - Evidence states the truthful zero-feature baseline; no claim of map/preview functionality
  </acceptance_criteria>
  <done>The Walking Skeleton is proven by the same ladder every later wave uses, with redacted committed evidence and an honest OPS-01 record. Later plans append features and manifest cases onto a green, manifest-driven baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Napplet iframe (opaque origin) → shell | All privileged access crosses here via the resource capability |
| Built artifact → network | Any direct egress outside the adapter path violates SBOX-03 |
| Kehto/Paja CLI → harness | Unverified CLI assumptions would carry all SBOX-02 evidence |
| Repository + dist + evidence → public GitHub | Built artifact, logs, and probe records are published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Elevation of Privilege | Napplet sandbox boundary | high | mitigate | Native single-file artifact, conformance without allow-same-origin, no direct fetch/XHR assumptions; verify-dist rejects remote executable assets |
| T-01-10 | Tampering | Shell boundary bypass in app code | high | mitigate | Static scan (verify-shell-boundary) plus dynamic loopback-only request-log assertion in every Paja case |
| T-01-11 | Denial of Service | Capability-absent boot | medium | mitigate | Feature-detected adapter with normalized errors; boot-denied case and conformance degraded pass |
| T-01-12 | Repudiation | Unverified CLI contract behind all SBOX-02 evidence | medium | mitigate | kehto probe recorded before harness use; blocked path goes to the OPS-01 ledger and the later checkpoint-gated fallback plan |
| T-01-13 | Spoofing | Executor drift from pinned defaults | medium | mitigate | defaults.test.ts asserts defaults.ts equals the approved v1-defaults.json field-by-field |
| T-01-SC | Tampering | npm/pnpm installs | high | mitigate | Zero installs in this plan; lock-approved check and lockfile-unchanged acceptance criterion; public-diff review at wave end |
</threat_model>

<verification>
- [ ] `bash scripts/verify-phase-01.sh` passes end-to-end (boot cases via manifest)
- [ ] Conformance JSON evidence exit 0 including degraded pass; verify-dist exit 0
- [ ] Paja boot + boot-denied evidence captured from the built artifact with loopback-only request logs
- [ ] kehto-cli-contract.json records the observed CLI contract (or the honest blocked state + ledger entry)
- [ ] Wave-end secret/public-diff gate: `pnpm scan:secrets` exits 0 over repo + dist and `git diff --check` clean; no lockfile delta in this plan's commits
</verification>

<success_criteria>
- Walking Skeleton committed and proven: pinned defaults, sole adapter seam, tokens/semantic truthful UI, native single-file build, independent verify-dist, conformance green, real-Paja boot with zero external egress
- The Kehto/Paja contract is observed evidence, not assumption; any blocker >30 minutes is a validated ledger entry with the fallback deferred to its checkpoint-gated plan
- The manifest-driven ladder is the single verification path and runs green (honest SKIPs only for later-plan validators)
</success_criteria>

<output>
Create `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-03-SUMMARY.md` when done
</output>
