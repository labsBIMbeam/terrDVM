---
phase: 01-sandboxed-bbox-and-orthophoto-ui
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - apps/napplet/src/bbox/validate.ts
  - apps/napplet/src/bbox/area.ts
  - apps/napplet/src/bbox/request-preview.ts
  - apps/napplet/src/ui/copy.ts
  - apps/napplet/src/ui/app.ts
  - apps/napplet/tests/unit/bbox-validation.test.ts
  - apps/napplet/tests/unit/bbox-area.test.ts
  - apps/napplet/tests/unit/request-preview.test.ts
  - apps/napplet/tests/unit/ui-copy.test.ts
autonomous: true
requirements: [MAP-03, MAP-04, MAP-07]
must_haves:
  truths:
    - "Named unit cases 1-9 pass with the exact pinned discriminator: after finite/range checks, west == east maps to ORDER (zero-width), west > east maps to ANTIMERIDIAN_AMBIGUOUS, south >= north maps to ORDER; the first-failing order is malformed -> non-finite -> range -> longitude discriminator (antimeridian-ambiguous or order) -> south/north order -> area limit"
    - "Geodesic area is proven against an independent oracle: a 1 degree x 1 degree box at the equator computes ~12,308 km2 within 0.5% (an absolute expected value, never the library compared to itself), and a high-latitude box matches the cos-latitude ratio — degree arithmetic is excluded"
    - "The canonical request preview DTO is a pure projection showing W,S,E,N in EPSG:4326, geodesic area, the pinned fixed defaults imported from defaults.ts (RES_M 5, OUTPUT_MIME model/gltf-binary), and exactly one source suffix (live / test fixture / local fallback / unavailable)"
    - "All five MAP-04 error strings plus toolbar, empty-state, toast, and success copy live in one copy module, are asserted verbatim against the UI-SPEC Copywriting Contract, and every validation code maps to exactly one UI string"
    - "The bbox domain modules and the copy module are DOM-free and network-free — no map library import, no adapter import, no UI rendering beyond the existing app.ts refactor to consume copy.ts"
  artifacts:
    - path: "apps/napplet/src/bbox/validate.ts"
      provides: "Total discriminated-result bbox normalize/validate with pinned clause order and codes MALFORMED | NON_FINITE | RANGE | ORDER | ANTIMERIDIAN_AMBIGUOUS | AREA_LIMIT"
      contains: "ANTIMERIDIAN_AMBIGUOUS"
    - path: "apps/napplet/src/bbox/area.ts"
      provides: "Audited geodesic area wrapper (WGS84 polygon area in km2) around @turf/area"
      contains: "@turf/area"
    - path: "apps/napplet/src/bbox/request-preview.ts"
      provides: "Canonical MAP-07 request preview DTO (pure projection, fixed v1 defaults, source suffix)"
      contains: "EPSG:4326"
    - path: "apps/napplet/src/ui/copy.ts"
      provides: "Single source of truth for all UI-SPEC Copywriting Contract strings plus the validation-code-to-copy mapping"
      contains: "No area selected"
    - path: "apps/napplet/tests/unit/bbox-validation.test.ts"
      provides: "Named cases 1-9 with the exact discriminator inputs"
    - path: "apps/napplet/tests/unit/ui-copy.test.ts"
      provides: "Verbatim copy assertions for MAP-04 strings, toolbar, empty state, toast, success, and fixed-default rows"
  key_links:
    - from: "apps/napplet/src/bbox/request-preview.ts"
      to: "apps/napplet/src/config/defaults.ts"
      via: "DTO imports RES_M and OUTPUT_MIME from the pinned defaults module — never local literals"
      pattern: "config/defaults"
    - from: "apps/napplet/src/bbox/area.ts"
      to: "@turf/area"
      via: "geodesic area delegated to the audited approved library"
      pattern: "@turf/area"
    - from: "apps/napplet/src/ui/copy.ts"
      to: "apps/napplet/src/bbox/validate.ts"
      via: "errorCopyFor(code) maps the exported validation-code union to exact UI-SPEC strings"
      pattern: "errorCopyFor"
    - from: "apps/napplet/src/ui/app.ts"
      to: "apps/napplet/src/ui/copy.ts"
      via: "the boot UI renders its strings from the copy module instead of inline literals"
      pattern: "ui/copy"
---

<objective>
Build the pure bbox domain — normalize/validate with the exact pinned ORDER/ANTIMERIDIAN discriminator, the audited geodesic area wrapper with an independent oracle, and the canonical MAP-07 request preview DTO — all TDD (RED before GREEN), plus the single copy module asserting every MAP-04 string and the toolbar/empty/toast copy verbatim. No map library import, no UI feature beyond wiring the existing boot UI to the copy module.

Purpose: MAP-03/04/07 are the phase's testable business logic. Pinning the discriminator (review Blocker 9), the independent area oracle (review Warning 2), and the verbatim copy tests (review Warning 1) here means Plan 05's map UI and Plan 06's preview pipeline consume proven, DOM-free domain modules instead of inventing semantics.
Output: validate.ts, area.ts, request-preview.ts, copy.ts (+ app.ts consuming it), and four unit test files with named cases 1-14 plus copy assertions. Zero lockfile mutations; zero new smoke cases (the manifest is untouched this wave).
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-03-SUMMARY.md
@apps/napplet/src/config/defaults.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD bbox normalize/validate — named cases 1-9 with the exact pinned ORDER/ANTIMERIDIAN discriminator</name>
  <files>apps/napplet/src/bbox/validate.ts, apps/napplet/tests/unit/bbox-validation.test.ts</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (Pattern 1 "Pure Bbox Domain Before Map Wiring", "Required Named Unit Cases" 1-9), .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md (Copywriting Contract invalid-bbox rows)</read_first>
  <precondition>Plan 03 complete: workspace, vitest, and the skeleton ladder are green; `node scripts/verify-lock-approved.mjs` exits 0. No install runs in this plan.</precondition>
  <behavior>
    Exact test names and code mapping (the review's Blocker 9 discriminator, now pinned — the executor decides nothing here):
    - Test 1 `bbox_rejects_non_array_or_wrong_arity`: null, a string, a 3-tuple, a 5-tuple, and an object each return code MALFORMED.
    - Test 2 `bbox_rejects_non_finite_each_position`: NaN, Infinity, and -Infinity substituted at each of the four positions each return NON_FINITE.
    - Test 3 `bbox_rejects_longitude_out_of_range`: west or east at -180.01 or 180.01 returns RANGE.
    - Test 4 `bbox_rejects_latitude_out_of_range`: south or north at -90.01 or 90.01 returns RANGE.
    - Test 5 `bbox_rejects_west_greater_or_equal_east`: exercises the EQUALITY branch — [10, 0, 10, 1] (west == east, zero-width) returns ORDER.
    - Test 6 `bbox_rejects_south_greater_or_equal_north`: [0, 10, 1, 5] and [0, 10, 1, 10] each return ORDER.
    - Test 7 `bbox_rejects_antimeridian_crossing_or_ambiguity`: exercises the STRICT-GREATER branch — [170, 0, -170, 1] (both longitudes in range, west > east) returns ANTIMERIDIAN_AMBIGUOUS; [170, 5, -170, 5] (west > east AND south >= north simultaneously) still returns ANTIMERIDIAN_AMBIGUOUS because the longitude discriminator is evaluated first.
    - Test 8 `bbox_rejects_zero_area`: [10, 0, 10, 1] (zero width) and [0, 5, 1, 5] (zero height) each return ORDER — zero area is fully absorbed into ORDER; there is no separate zero code.
    - Test 9 `bbox_rejects_configured_area_limit`: with an injected stub area function returning 100.01 and maxAreaKm2 100, the result is AREA_LIMIT; a stub returning exactly 100 passes (the limit is strictly "over").
    - A companion `bbox_accepts_valid_box` asserts [13.0, 47.0, 13.1, 47.1] returns ok with a normalized readonly [west, south, east, north] tuple.
  </behavior>
  <action>
  RED first: write `apps/napplet/tests/unit/bbox-validation.test.ts` with the named cases above against the not-yet-existing module; run `pnpm --filter @terrdvm/napplet test:unit` to confirm failure; commit (`test(01-04): add failing bbox validation cases 1-9`).

  GREEN: implement `apps/napplet/src/bbox/validate.ts` exporting `type BBox4326 = readonly [west: number, south: number, east: number, north: number]`, `type BBoxErrorCode = 'MALFORMED' | 'NON_FINITE' | 'RANGE' | 'ORDER' | 'ANTIMERIDIAN_AMBIGUOUS' | 'AREA_LIMIT'`, a discriminated `BBoxResult`, and `validateBBoxStructure(input: unknown, opts: { maxAreaKm2: number; areaKm2: (bbox: BBox4326) => number }): BBoxResult`. The area function is injected so this task stays independent of Task 2; Task 2 binds the real geodesic wrapper. Clause evaluation order is total and pinned — first failing clause wins, exactly:
  1. MALFORMED — not a 4-element array of numbers (wrong arity, wrong types, null/undefined).
  2. NON_FINITE — any position fails Number.isFinite.
  3. RANGE — west or east outside [-180, 180]; south or north outside [-90, 90].
  4. Longitude discriminator (evaluated before the latitude order check): west > east returns ANTIMERIDIAN_AMBIGUOUS (v1 rejects every crossing/ambiguous box — in [W,S,E,N] with both longitudes in range, crossing IS west > east); west == east returns ORDER (zero-width, not a crossing).
  5. south >= north returns ORDER (covers inverted and zero-height).
  6. AREA_LIMIT — areaKm2(bbox) strictly greater than maxAreaKm2.
  No silent coercion, no exception-based control flow for user input, no rounding before validation. The module imports nothing from ui/, map/, ortho/, or shell/ and performs no I/O. Commit GREEN (`feat(01-04): bbox validation with pinned discriminator`), refactor with tests green if needed.
  </action>
  <verify>
    <automated>pnpm --filter @terrdvm/napplet test:unit -- bbox-validation && pnpm --filter @terrdvm/napplet typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED commit precedes GREEN commit; all nine named cases pass with the exact names from 01-RESEARCH.md
    - Test 5 asserts the equality input maps to ORDER and test 7 asserts the strict-greater input maps to ANTIMERIDIAN_AMBIGUOUS, including the combined west>east + south>=north input
    - A comment-filtered scan of apps/napplet/src/bbox/validate.ts finds no reference to DOM globals, timers, or network APIs and no import outside the bbox directory and type-only imports
  </acceptance_criteria>
  <done>The total validation function exists with the pinned discriminator and clause order; every rejection clause of MAP-04 has a named passing test; the module is pure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD geodesic area wrapper with independent oracle + canonical request preview DTO with pinned fixed values</name>
  <files>apps/napplet/src/bbox/area.ts, apps/napplet/src/bbox/request-preview.ts, apps/napplet/tests/unit/bbox-area.test.ts, apps/napplet/tests/unit/request-preview.test.ts</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md ("Required Named Unit Cases" 10-14, Scope table rows MAP-03/MAP-07), .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md (Request preview panel rows, source-row suffixes), apps/napplet/src/config/defaults.ts, apps/napplet/src/bbox/validate.ts</read_first>
  <behavior>
    - Test 10 `bbox_area_is_geodesic_at_equator`: geodesicAreaKm2([0, 0, 1, 1]) is within 0.5% of the INDEPENDENT absolute oracle 12,308 km2 (accepted band 12,246.5 to 12,369.5). The expected value is a hardcoded number derived from WGS84 geometry, never a value computed by @turf/area itself — the library is checked against an external truth, not against itself.
    - Test 11 `bbox_area_is_geodesic_at_high_latitude`: geodesicAreaKm2([0, 60, 1, 61]) divided by geodesicAreaKm2([0, 0, 1, 1]) equals cos(60.5 degrees) (~0.492) within 2% — raw degree arithmetic would give a ratio of ~1.0 and fails this test by an order of magnitude.
    - Named case 9 re-run against the real wrapper: validateBBox([0, 0, 0.2, 0.2]) (~492 km2) returns AREA_LIMIT with MAX_AREA_KM2 100, and [0, 0, 0.09, 0.09] (~99.7 km2) passes.
    - Test 12 `request_preview_uses_west_south_east_north_epsg4326`: the DTO exposes bbox fields in W,S,E,N order with display strings formatted to exactly 6 decimal places, and `crs` is exactly "EPSG:4326".
    - Test 13 `request_preview_uses_fixed_resolution_and_output`: the DTO's resolution is 5 and output MIME is "model/gltf-binary", read from the defaults module — the test additionally asserts the DTO values are identical to the exports of src/config/defaults.ts (which Plan 03's defaults.test.ts already pins to the approved v1-defaults.json).
    - Test 14 `request_preview_names_active_source_or_fallback`: for source states live / fixture / local-fallback / none, the DTO source row renders exactly one of the suffixes "— live", "— test fixture", "— local fallback", "— unavailable" — never two, never empty.
  </behavior>
  <action>
  RED first: write both test files against the not-yet-existing modules and commit failing. GREEN:

  `apps/napplet/src/bbox/area.ts`: export `geodesicAreaKm2(bbox: BBox4326): number` that builds the closed GeoJSON polygon [[w,s],[e,s],[e,n],[w,n],[w,s]], delegates to `@turf/area` (approved pin 7.3.5, installed in Plan 02), and divides by 1,000,000. No hand-rolled spherical math, no degree-product formula anywhere. Also export `validateBBox(input: unknown): BBoxResult` composing Task 1's `validateBBoxStructure` with `geodesicAreaKm2` and `MAX_AREA_KM2` from `src/config/defaults.ts` — this is the single entry point later plans call.

  `apps/napplet/src/bbox/request-preview.ts`: export a pure `buildRequestPreview(bbox: BBox4326, areaKm2: number, source: SourceState): RequestPreviewDTO` — no signing, no publication, no I/O. The DTO carries: bbox values in W,S,E,N order plus 6-decimal display strings; `crs: 'EPSG:4326'`; `areaKm2` (display formatted to 1 decimal place with thousands separators per UI-SPEC overflow rules); `resolutionM` and `outputMime` imported from `src/config/defaults.ts` (RES_M 5, OUTPUT_MIME model/gltf-binary — never re-declared locally); and a source row of `{ name: string; suffix: 'live' | 'test fixture' | 'local fallback' | 'unavailable' }` where exactly one suffix is derived from the SourceState union. Commit GREEN, refactor with tests green.
  </action>
  <verify>
    <automated>pnpm --filter @terrdvm/napplet test:unit -- bbox-area request-preview && pnpm --filter @terrdvm/napplet typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Test 10 asserts the literal oracle band around 12,308 km2; the string "12308" (or its formatted equivalent) appears in the test file as a constant, not derived from a turf call
    - Test 11's ratio assertion excludes degree arithmetic; tests 12-14 pass with the exact names
    - request-preview.ts imports RES_M and OUTPUT_MIME from config/defaults and contains no numeric resolution literal and no MIME literal of its own
    - bbox/ modules remain pure: comment-filtered scan finds no DOM global, no adapter import, no network API
  </acceptance_criteria>
  <done>Geodesic area is proven against an independent oracle at equator and high latitude, the composed validateBBox enforces the area limit with real geodesic values, and the canonical MAP-07 DTO is a pure, pinned-default, single-suffix projection ready for UI consumption in Plan 05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Copy module + verbatim UI-copy tests for all MAP-04 strings, toolbar, empty state, and toast; wire the boot UI to it</name>
  <files>apps/napplet/src/ui/copy.ts, apps/napplet/src/ui/app.ts, apps/napplet/tests/unit/ui-copy.test.ts</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md (Copywriting Contract — complete section), apps/napplet/src/ui/app.ts, apps/napplet/src/config/defaults.ts, apps/napplet/src/bbox/validate.ts</read_first>
  <behavior>
    - `ui_copy_matches_contract_for_every_map04_error`: errorCopyFor('MALFORMED') and errorCopyFor('NON_FINITE') both return "Coordinates must be finite decimal degrees. Check each field for empty values or stray characters."; errorCopyFor('RANGE') returns "Longitude must be between −180 and 180, latitude between −90 and 90."; errorCopyFor('ORDER') returns "West must be less than east, and south must be less than north."; errorCopyFor('ANTIMERIDIAN_AMBIGUOUS') returns "Selections crossing the ±180° line are not supported in this version. Draw a rectangle that stays on one side of the antimeridian."; errorCopyFor('AREA_LIMIT', { areaKm2, maxAreaKm2 }) interpolates "Selected area is {AREA} km², over the {MAX_AREA_KM2} km² limit for this service. Draw a smaller rectangle or lower the coordinates' span." with MAX_AREA_KM2 from defaults (100).
    - `ui_copy_matches_contract_for_buttons_and_states`: the seven CTA strings ("Draw bounding box", "Stop drawing", "Enter coordinates", "Apply coordinates", "Clear selection", "Restore selection", "Retry preview"), the empty-state heading "No area selected" and its body, the toast "Selection cleared.", the success template "Selection ready. {AREA} km² — the request preview below reflects this selection.", the loading string, the fixed-default rows "Resolution: 5 m/px — fixed for v1" / "Output: model/gltf-binary — fixed for v1" (rendered via defaults, not literals), and the helper caption are all asserted verbatim.
    - `ui_copy_covers_transport_states`: denied, timeout (with TIMEOUT_S 15 interpolated), generic failed, source-unavailable, offline, fixture badge "TEST FIXTURE" + body, and the request-panel title/bbox label strings match the contract verbatim (these are consumed by Plan 06; asserting them now freezes the copy).
  </behavior>
  <action>
  RED first: write `apps/napplet/tests/unit/ui-copy.test.ts` with the assertions above (quote every expected string verbatim from the UI-SPEC Copywriting Contract — the test file is the frozen copy oracle) and commit failing. GREEN:

  `apps/napplet/src/ui/copy.ts`: export a single `COPY` object holding every Copywriting Contract string (buttons, empty state, helpers, toast, success, loading, all five validation errors, denied/timeout/failed/unavailable/offline, fixture badge and body, fallback transport label, request-panel labels, source-row suffixes) with typed interpolation functions for {AREA}, {MAX_AREA_KM2}, {TIMEOUT_S}, {SOURCE_ATTRIBUTION}, {LICENSE_ID}, {SOURCE_NAME} placeholders — placeholder values always arrive from `config/defaults.ts` or the source-policy record, never hardcoded in components. Export `errorCopyFor(code: BBoxErrorCode, params?)` mapping each validation code to exactly one contract string (MALFORMED and NON_FINITE share the finite-decimal-degrees string per the UI-SPEC "malformed / non-finite" row). The module is pure — string data and formatting only.

  Refactor `apps/napplet/src/ui/app.ts`: replace every inline UI string from Plan 03's boot UI with references to COPY (empty state, request panel labels, fixed-default rows, source row, unavailable state). Rendered output must be byte-identical to before — the Plan 03 Paja boot cases must still pass unchanged, proving the refactor changed wiring, not copy. Commit GREEN, refactor with tests green.

  No smoke case is added this wave, so `apps/napplet/tests/smoke/cases.json` is intentionally untouched and the runner needs no update — the wave-end gate below still runs the full manifest ladder.
  </action>
  <verify>
    <automated>pnpm --filter @terrdvm/napplet test:unit -- ui-copy && pnpm --filter @terrdvm/napplet build && node scripts/verify-dist.mjs && node apps/napplet/tests/smoke/paja-harness.mjs --case boot</automated>
  </verify>
  <acceptance_criteria>
    - ui-copy.test.ts quotes every asserted string verbatim from the UI-SPEC and passes; every BBoxErrorCode maps to exactly one string
    - app.ts contains no inline user-facing string literal that duplicates a COPY entry; the Paja boot case still passes after the refactor
    - copy.ts is pure (comment-filtered scan: no DOM global, no import from map/ortho/shell modules)
  </acceptance_criteria>
  <done>One copy module is the single source of truth for all Phase 1 UI strings, all MAP-04 copy plus toolbar/empty/toast copy is frozen by verbatim tests, and the boot UI consumes it without visible change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User coordinate input → domain validation | Untrusted free-form values enter the bbox validator |
| Domain results → UI copy | Validation codes select user-facing strings; a wrong mapping misleads the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-21 | Tampering | Bbox input integrity | high | mitigate | Total discriminated-result validation with pinned first-failing clause order; named cases 1-9 cover every rejection clause including the exact ORDER/ANTIMERIDIAN_AMBIGUOUS discriminator |
| T-01-22 | Tampering | Degree-area shortcut producing wrong km2 | medium | mitigate | Independent 12,308 km2 equator oracle plus cos-latitude ratio test; wrapper delegates to audited @turf/area only |
| T-01-23 | Spoofing | DTO misstating defaults or source state | medium | mitigate | Cases 12-14 assert defaults are imported from the pinned defaults module and exactly one source suffix renders |
| T-01-24 | Information Disclosure | Copy drift mislabeling degraded states | medium | mitigate | Verbatim frozen copy tests including transport-state strings; errorCopyFor is the only code-to-copy path |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero installs in this plan; lockfile unchanged (verify-lock-approved in the runner); public-diff review at wave end |
</threat_model>

<verification>
- [ ] `pnpm --filter @terrdvm/napplet test:unit` green — named cases 1-14 plus the three ui-copy tests
- [ ] `pnpm --filter @terrdvm/napplet build && node scripts/verify-dist.mjs` and conformance still green after the app.ts refactor
- [ ] `bash scripts/verify-phase-01.sh` passes (manifest boot cases unchanged; source-policy/local-surface still honest SKIPs)
- [ ] Wave-end secret/public-diff gate: `pnpm scan:secrets` exits 0 and `git diff --check` clean; no lockfile delta in this plan's commits
</verification>

<success_criteria>
- MAP-04 semantics are pinned and proven: equality is ORDER, strict-greater is ANTIMERIDIAN_AMBIGUOUS, zero area is ORDER, and the clause order is deterministic
- MAP-03 area is geodesic with an independent absolute oracle; MAP-07's DTO is pure, pinned, and single-suffix
- All UI copy needed by Plans 05-06 is frozen verbatim in one module with passing tests; domain modules stay DOM- and network-free
</success_criteria>

<output>
Create `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-04-SUMMARY.md` when done
</output>
