---
phase: 01-sandboxed-bbox-and-orthophoto-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/evidence/phase-01/map-provenance.json
  - .planning/evidence/phase-01/README.md
  - .planning/evidence/fallback-ledger.jsonl
  - .planning/evidence/phase-01/package-audit.md
  - .planning/evidence/phase-01/v1-defaults.json
  - scripts/verify-map-provenance.mjs
  - scripts/validate-fallback-ledger.mjs
  - scripts/verify-package-audit.mjs
  - scripts/verify-v1-defaults.mjs
autonomous: false
requirements: [MAP-08, OPS-01]
must_haves:
  truths:
    - "A five-field MAP-08 provenance decision exists on disk before any map code is written, and it records the clean-room fallback unless all five 21maps fields independently verified"
    - "The OPS-01 fallback ledger exists at its fixed location with a fixed validated schema from Wave 0, empty rather than faked"
    - "A machine-verified package dossier covers every Phase 1 direct dependency — including direct playwright@1.59.1 with its Chromium provisioning story — with per-package repo/license/tarball/scripts/peer evidence, per-plan install location, and zero packages outside the approved list"
    - "The fixed v1 defaults RES_M=5, OUTPUT_MIME=model/gltf-binary, MAX_AREA_KM2=100, TIMEOUT_S=15 are pinned in a validated decision artifact — no executor-chosen values anywhere in Phase 1"
    - "The blocking human checkpoint approves ALL Phase 1 installs (the single Plan 02 lockfile mutation plus Chromium provisioning) and the four v1 defaults; no package-manager install or lockfile exists anywhere in the repository before that approval"
  artifacts:
    - path: ".planning/evidence/phase-01/map-provenance.json"
      provides: "MAP-08 decision record (clean-room fallback or five verified provenance fields)"
      contains: "\"requirement\": \"MAP-08\""
    - path: ".planning/evidence/fallback-ledger.jsonl"
      provides: "OPS-01 append-only fallback activation ledger (valid when empty)"
    - path: "scripts/validate-fallback-ledger.mjs"
      provides: "Schema validator for the ledger (schema_version 1, RFC3339 timestamps, elapsed_minutes >= 30 for time triggers, repo-relative evidence paths)"
    - path: "scripts/verify-map-provenance.mjs"
      provides: "Validator that fails unless the decision is clean-room-fallback, or verified-reuse with all five fields non-empty"
    - path: ".planning/evidence/phase-01/package-audit.md"
      provides: "Complete Phase 1 package dossier: 14 exact pins, direct Playwright, Kehto/Paja existing-install audit, Chromium provisioning, per-plan use, exclusion list"
      contains: "playwright"
    - path: ".planning/evidence/phase-01/v1-defaults.json"
      provides: "Pinned fixed v1 request defaults (MAP-07 planning decision made concrete)"
      contains: "model/gltf-binary"
    - path: "scripts/verify-package-audit.mjs"
      provides: "Validator asserting one dossier row per required package with exact pin and required evidence columns, and no row outside the list"
    - path: "scripts/verify-v1-defaults.mjs"
      provides: "Validator asserting the four default values exactly (RES_M 5, OUTPUT_MIME model/gltf-binary, MAX_AREA_KM2 100, TIMEOUT_S 15)"
  key_links:
    - from: "scripts/verify-map-provenance.mjs"
      to: ".planning/evidence/phase-01/map-provenance.json"
      via: "validator reads and enforces the decision record"
      pattern: "map-provenance\\.json"
    - from: "scripts/validate-fallback-ledger.mjs"
      to: ".planning/evidence/fallback-ledger.jsonl"
      via: "validator parses every JSONL line against the fixed schema"
      pattern: "fallback-ledger\\.jsonl"
    - from: "scripts/verify-package-audit.mjs"
      to: ".planning/evidence/phase-01/package-audit.md"
      via: "validator asserts every required pin row and rejects extra packages"
      pattern: "package-audit\\.md"
    - from: "scripts/verify-v1-defaults.mjs"
      to: ".planning/evidence/phase-01/v1-defaults.json"
      via: "validator asserts exact default values"
      pattern: "v1-defaults\\.json"
---

<objective>
Close the fail-closed Phase 1 entry gates before any application code or dependency exists: (1) the MAP-08 21maps provenance decision, defaulting to a recorded clean-room fallback; (2) OPS-01 fallback-ledger infrastructure at its fixed location with a fixed schema; (3) a complete, machine-verified package-legitimacy dossier covering every Phase 1 direct dependency — including the direct `playwright@1.59.1` pin the smoke harness needs — plus the pinned fixed v1 request defaults; and (4) one blocking human checkpoint that approves ALL Phase 1 installs and the defaults in a single decision.

Purpose: MAP-08 and OPS-01 are roadmap entry gates; the package-legitimacy gate is mandatory before any `pnpm` install in a public repository; and MAP-07's fixed v1 defaults are a recorded planning decision, not an executor choice. Nothing downstream may start on an unverified base, and Plan 02's single lockfile mutation must be fully authorized before it runs.
Output: provenance record + validator, fallback ledger + validator, evidence README, package dossier + validator, v1-defaults record + validator, and recorded human approval. No `package.json`, no lockfile, no dependency install in this plan.
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
@$HOME/.hermes/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record MAP-08 clean-room fallback decision and create the OPS-01 fallback ledger with validators</name>
  <files>.planning/evidence/phase-01/map-provenance.json, .planning/evidence/phase-01/README.md, .planning/evidence/fallback-ledger.jsonl, scripts/verify-map-provenance.mjs, scripts/validate-fallback-ledger.mjs</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (sections "Open Gates Before Implementation" Gate G1 and "OPS-01 Fallback Ledger Contract")</read_first>
  <action>
  Create `.planning/evidence/phase-01/map-provenance.json` recording the MAP-08 decision. Per Gate G1, the existing research trace found no definitive 21maps repository or local source, so the default recorded decision is `"decision": "clean-room-fallback"` with fields: `requirement: "MAP-08"`, `decision`, `decided_at` (RFC3339 UTC), `reason` (one sentence: provenance unresolved at planning time), and an `evidence` object with the five provenance fields (`source_location`, `version_identity`, `license`, `provenance_chain`, `maplibre_pin`) set to null for the fallback case. Consequence recorded in the file: the Napplet uses a clean MapLibre-compatible implementation; 21maps code, assets, styles, and configuration are not copied. Exception clause (record verbatim in the JSON as `reuse_condition`): if immutable 21maps provenance is independently found during execution BEFORE any map code exists, the decision may be flipped to `"verified-reuse"` only when all five evidence fields are non-empty and pass; the plan does not depend on that happening.

  Create `scripts/verify-map-provenance.mjs` (plain Node, zero dependencies — no package.json exists yet): exits 0 only if the JSON parses, `requirement` is MAP-08, and either `decision` is `clean-room-fallback`, or `decision` is `verified-reuse` with all five `evidence` fields non-empty strings. Any other shape exits 1 with the failing field named.

  Create `.planning/evidence/fallback-ledger.jsonl` as a zero-byte file (fixed OPS-01 location; empty-but-valid is the honest state — never add a fake activation).

  Create `scripts/validate-fallback-ledger.mjs` (plain Node, zero dependencies) enforcing the schema from 01-RESEARCH.md "OPS-01 Fallback Ledger Contract": each line is a JSON object with `schema_version: 1`; `occurred_at`/`started_at` parse as RFC3339 and `started_at <= occurred_at`; `elapsed_minutes >= 30` when `trigger` is the 30-minute blocker law; `fallback_id`, `reason`, `decided_by`, `user_visible_state` non-empty; `evidence_paths` are repository-relative without `..` traversal; `outcome` in {activated, resolved, failed-closed}; unknown `schema_version` fails. A zero-byte file exits 0 and prints `ledger valid: 0 entries`.

  Create `.planning/evidence/phase-01/README.md` documenting the evidence conventions: all evidence in this directory is committed to a public repository; secrets, tokens, authorization headers, and token-bearing URLs are replaced with `[REDACTED]` before commit; evidence reports themselves are rescanned by the secret scanner once it exists (Plan 02).

  Do NOT build the package audit in this task — the dossier is Task 2's deliverable so the checkpoint in Task 3 reviews a complete, separately verified artifact.
  </action>
  <verify>
    <automated>node scripts/verify-map-provenance.mjs && node scripts/validate-fallback-ledger.mjs</automated>
  </verify>
  <acceptance_criteria>
    - map-provenance.json contains "clean-room-fallback" (or verified-reuse with all five evidence fields populated and independently verified)
    - fallback-ledger.jsonl exists and validate-fallback-ledger.mjs exits 0 reporting 0 entries
    - Both validators are plain-Node scripts runnable without any installed dependency
  </acceptance_criteria>
  <done>MAP-08 decision is durably recorded before any map code exists; the OPS-01 ledger and both validators exist and pass. Map implementation tasks in later plans are unblocked only through this record.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the complete package dossier and pin the fixed v1 defaults, each with a machine validator</name>
  <files>.planning/evidence/phase-01/package-audit.md, .planning/evidence/phase-01/v1-defaults.json, scripts/verify-package-audit.mjs, scripts/verify-v1-defaults.mjs</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (sections "Package Legitimacy Audit", "Standard Stack", "Installation Gate"), /tmp/npm-packs/pack-output.json (packed-tarball identities for the Napplet trio, if still present)</read_first>
  <action>
  Create `.planning/evidence/phase-01/package-audit.md` — the single dossier the Task 3 checkpoint reviews and the sole authorization source for every Phase 1 install. One row per package with columns: exact registry name@version, npmjs.com/package URL, official source repository and ownership, license, tarball identity (use `/tmp/npm-packs/pack-output.json` and the extracted packages for the Napplet trio where available), lifecycle-script findings (the research trace captured none — re-check per package), peer-range reconciliation notes, install location, and which plan uses it. Execution MAY perform read-only registry/repository checks (`npm view <pkg>@<version>`, repository pages) to fill or confirm rows; it MUST NOT run any install, `pnpm add`, or lockfile-creating command.

  The exact approved pin list — all installed in Plan 02's ONE lockfile mutation, nothing installed in any later plan:

  Root devDependencies (lint toolchain, config at repo root): `eslint@9.39.2`, `@eslint/js@9.39.2`, `typescript-eslint@8.46.4`.
  apps/napplet dependencies: `@napplet/sdk@0.25.0`, `maplibre-gl@5.24.0`, `terra-draw@1.32.2`, `terra-draw-maplibre-gl-adapter@1.4.1`, `@turf/area@7.3.5`.
  apps/napplet devDependencies: `@napplet/vite-plugin@0.12.0`, `@napplet/conformance-cli@0.2.16`, `playwright@1.59.1`, `vite@8.1.5`, `typescript@5.9.3`, `vitest@4.1.10`.

  Per-plan use column: Plan 02 installs everything and provisions Chromium; Plan 03 consumes @napplet/sdk (adapter), @napplet/vite-plugin + vite (single-file build), @napplet/conformance-cli (conformance), playwright (Paja harness), vitest + typescript (tests/typecheck); later plans consume maplibre-gl, terra-draw, terra-draw-maplibre-gl-adapter, @turf/area for map/bbox work — approved now, installed in Plan 02, first imported later.

  Dedicated dossier section "Direct Playwright and Chromium provisioning": the smoke harness imports Playwright directly and MUST NEVER import a transitive dependency; the pin is `playwright@1.59.1` because the locally captured `@napplet/conformance-cli@0.2.16` declares peer/dependency `playwright ^1.59.1` (record the exact declaration found in the local packed artifact). Chromium is provisioned in Plan 02 via `pnpm --filter @terrcvm/napplet exec playwright install chromium` only after this checkpoint's approval; OS-dependency installation (`playwright install --with-deps` or distro packages) is NOT approved by this dossier and would require its own explicit approval.

  Dedicated dossier section "Existing Kehto/Paja installation (audited, NOT project-installed)": `@kehto/cli@0.2.16` (executable at the locally observed path, e.g. `/home/flx/.deno/bin/kehto`) and `@kehto/paja@0.8.0` (Deno cache). Record repository/license/version evidence from the local cache; these are never added to any package.json or lockfile and are never reinstalled by this project.

  Dedicated dossier section "Excluded — zero packages outside the list": `vite-plugin-singlefile` (redundant — `@napplet/vite-plugin@0.12.0` provides native `artifactMode: 'single-file'`), shadcn/any component registry, remote fonts/icons/assets, and every Phase 2+ package (nostr-tools app use, Zod protocol schemas, SQLite drivers, LNbits/Phoenixd clients, three.js, glTF validators, Blossom clients, Python/GDAL stack). Any dependency name not in the approved list fails the audit validator and, later, the phase gate.

  Create `scripts/verify-package-audit.mjs` (plain Node, zero dependencies): parses the dossier and exits 0 only if (a) every one of the 14 approved packages has a row with its exact pin string present, (b) every row has non-empty registry URL, repository, license, tarball/script findings, and install-location cells, (c) the two Kehto/Paja audit rows exist and are marked not-project-installed, (d) the Playwright/Chromium provisioning section and the exclusion section exist, and (e) no dependency row names a package outside the approved-plus-Kehto set. Exit 1 naming every missing/extra row.

  Create `.planning/evidence/phase-01/v1-defaults.json` pinning the fixed v1 request defaults (the MAP-07 recorded planning decision made concrete — these are decided HERE, not by any executor): `{ "schema_version": 1, "requirement": "MAP-07", "decided_at": "<RFC3339 UTC>", "RES_M": 5, "OUTPUT_MIME": "model/gltf-binary", "MAX_AREA_KM2": 100, "TIMEOUT_S": 15, "notes": { "RES_M": "5 m/px is the v1 default; 10 m DTM remains the PROC-06 fallback, never the default", "OUTPUT_MIME": "requested terrain artifact MIME — matches Phase 2 GLB 2.0 delivery (DUM-01), not a preview image type" } }`. Later plans' `apps/napplet/src/config/defaults.ts` must mirror these values exactly.

  Create `scripts/verify-v1-defaults.mjs` (plain Node, zero dependencies): exits 0 only if the JSON parses with `schema_version` 1, `requirement` "MAP-07", `RES_M` exactly 5, `OUTPUT_MIME` exactly "model/gltf-binary", `MAX_AREA_KM2` exactly 100, `TIMEOUT_S` exactly 15, and a parseable `decided_at`. Any drift exits 1 naming the field.
  </action>
  <verify>
    <automated>node scripts/verify-package-audit.mjs && node scripts/verify-v1-defaults.mjs</automated>
  </verify>
  <acceptance_criteria>
    - package-audit.md contains one evidence-complete row for each of the 14 exact pins, the Kehto/Paja audit rows, the direct-Playwright/Chromium section, and the exclusion section; verify-package-audit.mjs exits 0
    - v1-defaults.json pins RES_M 5, OUTPUT_MIME model/gltf-binary, MAX_AREA_KM2 100, TIMEOUT_S 15; verify-v1-defaults.mjs exits 0
    - No install, package.json, or lockfile was created (git status shows only the dossier, defaults, and validators)
  </acceptance_criteria>
  <done>The complete dossier and the pinned defaults exist as machine-verified artifacts, ready for human review. Every install Plan 02 will perform, including direct Playwright and Chromium provisioning, is documented with evidence before any lockfile exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint — human approval of ALL Phase 1 installs and the fixed v1 defaults before any lockfile mutation</name>
  <what-built>Two machine-verified decision artifacts: (1) `.planning/evidence/phase-01/package-audit.md` — the complete Phase 1 package dossier covering the 14 exact pins (root lint toolchain: eslint@9.39.2, @eslint/js@9.39.2, typescript-eslint@8.46.4; app dependencies: @napplet/sdk@0.25.0, maplibre-gl@5.24.0, terra-draw@1.32.2, terra-draw-maplibre-gl-adapter@1.4.1, @turf/area@7.3.5; app devDependencies: @napplet/vite-plugin@0.12.0, @napplet/conformance-cli@0.2.16, playwright@1.59.1, vite@8.1.5, typescript@5.9.3, vitest@4.1.10), the direct-Playwright rationale (conformance CLI declares playwright ^1.59.1; the harness never imports a transitive dep) with Chromium provisioning via `pnpm exec playwright install chromium` and NO OS-dependency install, the existing @kehto/cli@0.2.16 / @kehto/paja@0.8.0 audit (not project-installed), and the exclusion list (no vite-plugin-singlefile, no shadcn/registry, no remote assets, no Phase 2+ packages). (2) `.planning/evidence/phase-01/v1-defaults.json` — the fixed v1 request defaults RES_M=5 m/px, OUTPUT_MIME=model/gltf-binary, MAX_AREA_KM2=100, TIMEOUT_S=15. Both validators (`verify-package-audit.mjs`, `verify-v1-defaults.mjs`) exit 0. This approval authorizes ALL Phase 1 installs — the single Plan 02 lockfile mutation plus Chromium provisioning — not just one plan's subset. No later plan installs anything.</what-built>
  <how-to-verify>Open `.planning/evidence/phase-01/package-audit.md`. For each SUS package confirm the npmjs.com page, repository ownership, version, license, and lifecycle-script findings match the dossier; spot-check the Napplet trio against the packed tarballs under `/tmp/npm-packs/` if present. Confirm the pin list is complete, that playwright@1.59.1 appears as a DIRECT devDependency with the Chromium provisioning command recorded, that Kehto/Paja are audited but not project-installed, and that nothing outside the list appears. Then open `.planning/evidence/phase-01/v1-defaults.json` and confirm the four values (5 / model/gltf-binary / 100 / 15) are the intended v1 defaults — OUTPUT_MIME is the requested terrain artifact type that Phase 2 delivers, not an image type. No `pnpm add`/`pnpm install` may run anywhere until you approve.</how-to-verify>
  <resume-signal>Type "approved" to authorize exactly these pins for ALL Phase 1 installs (Plan 02's single lockfile mutation + Chromium provisioning) and these four defaults — or name each rejected package or default value (rejected packages are replaced or their dependent tasks fail closed; rejected defaults are re-pinned here before any code consumes them).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repository | Third-party package code enters the public project via install |
| External map source → repository | 21maps code/assets of unknown license/provenance could enter the codebase |
| Evidence files → public GitHub | Everything under `.planning/evidence/` is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | 21maps reuse path | high | mitigate | Task 1 records clean-room fallback; verify-map-provenance.mjs fails any reuse without five non-empty verified fields |
| T-01-02 | Repudiation | OPS-01 fallback activations | medium | mitigate | Fixed-location append-only JSONL ledger with schema validator from Wave 0; empty-but-valid, never faked |
| T-01-03 | Information Disclosure | Evidence directory in public repo | high | mitigate | README redaction policy in Task 1; scanner + rescan wired in Plan 02 and enforced at every wave end |
| T-01-04 | Spoofing | Executor-invented defaults reaching the signed-request preview | medium | mitigate | v1-defaults.json pinned in Task 2, human-approved in Task 3, machine-validated; defaults.ts must mirror it exactly |
| T-01-SC | Tampering | npm installs (all Phase 1 packages incl. direct playwright) | high | mitigate | Auto-built dossier (Task 2) + blocking human checkpoint (Task 3) covering every Phase 1 install before the first lockfile mutation; exact pins only; checkpoint is never auto-approvable |
</threat_model>

<verification>
- [ ] `node scripts/verify-map-provenance.mjs` exits 0
- [ ] `node scripts/validate-fallback-ledger.mjs` exits 0 with 0 entries
- [ ] `node scripts/verify-package-audit.mjs` and `node scripts/verify-v1-defaults.mjs` exit 0
- [ ] Human approval recorded covering all Phase 1 installs and the four defaults
- [ ] `git status` shows no package.json / pnpm-lock.yaml anywhere (no install happened in this plan)
- [ ] Wave-end secret/public-diff gate: `git diff --check` clean; committed files contain no secret-like material (manual review until the Plan 02 scanner exists)
</verification>

<success_criteria>
- MAP-08 gate closed fail-closed (clean-room fallback recorded, or verified reuse with complete evidence)
- OPS-01 ledger and validators exist and pass
- The dossier and defaults are machine-verified AND human-approved; the approval scope explicitly covers every Phase 1 install; no install occurred before approval
</success_criteria>

<output>
Create `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-01-SUMMARY.md` when done
</output>
