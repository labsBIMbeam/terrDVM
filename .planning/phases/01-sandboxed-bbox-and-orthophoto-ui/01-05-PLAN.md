---
phase: 01-sandboxed-bbox-and-orthophoto-ui
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - scripts/audit-source-candidates.mjs
  - scripts/verify-source-policy.mjs
  - scripts/generate-ortho-fixture.mjs
  - apps/napplet/src/config/source-policy.json
  - apps/napplet/tests/fixtures/ortho-fixture.png
  - apps/napplet/tests/fixtures/ortho-fixture-provenance.json
  - apps/napplet/src/map/map-view.ts
  - apps/napplet/src/map/source.ts
  - apps/napplet/src/ui/selection.ts
  - apps/napplet/src/ui/app.ts
  - apps/napplet/tests/unit/source-policy.test.ts
  - apps/napplet/tests/unit/selection.test.ts
  - apps/napplet/tests/smoke/paja-harness.mjs
  - apps/napplet/tests/smoke/cases.json
  - .planning/evidence/*
autonomous: false
requirements: [MAP-01, MAP-02, MAP-06, MAP-08]
must_haves:
  truths:
    - "Gate G2 has an achievement path, not only detection: at least 5 authoritative public/licensed candidates PER ROLE (basemap and imagery) were tested live against the full endpoint contract during execution, stopping at the first fully passing candidate per role; previously failed candidates count only when retested in this run, and an operator-supplied source descriptor (credential by shell reference only, value never recorded) is a first-class candidate input"
    - "apps/napplet/src/config/source-policy.json holds exactly two roles — basemap and imagery — each with every contract field (scheme/host/port/path template, layer, tile matrix/CRS/bbox order, format, auth, CORS/shell behavior, attribution string, terms, rate limits, max bytes, timeout, redirects, coverage bounds, outage behavior, capture date, status), and scripts/verify-source-policy.mjs validates the schema in default mode and in --require-live mode passes only when BOTH roles are live-verified, otherwise printing the named failed fields per role and exiting non-zero as a blocked outcome — never PASS"
    - "The blocking human source-policy checkpoint is success-oriented and fail-closed: full approval requires both roles live-verified; basemap-only approval may unlock only MAP-01/MAP-02 Task 3 when basemap is live-verified while imagery remains explicitly blocked, and can never count as MAP-05/MAP-06 completion; any >30-minute blocked role is recorded in the OPS-01 ledger before re-test or acknowledged-blocked/re-scope"
    - "After approval the built artifact draws, edits, and clears exactly one rectangle (Terra Draw on MapLibre, clean-room per the recorded MAP-08 provenance decision), with the coordinate-field + Apply coordinates keyboard path, the exact approved UI-SPEC tokens/copy/accessibility contract, and approved basemap tiles reaching MapLibre ONLY through the sole shell adapter via a registered custom protocol"
    - "The Paja request log for every new smoke case shows zero direct egress — no MapLibre worker/image/CSS/fetch request leaves the loopback/runtime allowlist — and the map overlay renders the basemap attribution as its own string with no dangling imagery separator while no imagery is rendered"
    - "No imagery preview is implemented in this plan; the self-generated deterministic fixture exists for tests only, always labeled TEST FIXTURE in provenance, and is never presented as live imagery or as source-achievement evidence"
  artifacts:
    - path: "scripts/audit-source-candidates.mjs"
      provides: "Bounded live candidate audit (>=5 per role, capped requests, full contract capture, --descriptor operator input, credential reference only)"
      contains: "descriptor"
    - path: "apps/napplet/src/config/source-policy.json"
      provides: "Exactly two-role non-secret source policy record with per-role attribution strings and full contract fields"
      contains: "imagery"
    - path: "scripts/verify-source-policy.mjs"
      provides: "Schema validation (default) and both-roles-live-verified gate (--require-live) with named failed fields and blocked outcome"
      contains: "--require-live"
    - path: "apps/napplet/tests/fixtures/ortho-fixture-provenance.json"
      provides: "Self-generated deterministic fixture provenance: generator hash, parameters, date, TEST FIXTURE statement, never-acceptance clause"
      contains: "TEST FIXTURE"
    - path: "apps/napplet/src/map/source.ts"
      provides: "Policy allowlist for both roles + basemap tile URL construction + attribution composition (separate strings, no dangling separator)"
      contains: "basemap"
    - path: "apps/napplet/src/map/map-view.ts"
      provides: "Clean-room MapLibre map with inline style, custom protocol routing every tile through the sole adapter, Terra Draw rectangle mode"
    - path: "apps/napplet/src/ui/selection.ts"
      provides: "Pure selection state machine (EMPTY/DRAWING/SELECTED_VALID/SELECTED_INVALID/EDITING/CLEARED_UNDOABLE) per the UI-SPEC interaction contract"
    - path: "apps/napplet/tests/smoke/cases.json"
      provides: "Manifest extended with draw, edit-clear, and coords-keyboard built-artifact cases"
      contains: "edit-clear"
  key_links:
    - from: "apps/napplet/src/map/map-view.ts"
      to: "apps/napplet/src/shell/resource-client.ts"
      via: "maplibregl.addProtocol handler that resolves every tile request through source.ts allowlisting and the sole adapter — MapLibre performs no direct fetch"
      pattern: "addProtocol"
    - from: "apps/napplet/src/map/source.ts"
      to: "apps/napplet/src/config/source-policy.json"
      via: "allowlist and attribution derive exclusively from the checked-in two-role policy record"
      pattern: "source-policy"
    - from: "apps/napplet/src/ui/selection.ts"
      to: "apps/napplet/src/bbox/area.ts"
      via: "every release/apply transition validates through validateBBox before SELECTED_VALID"
      pattern: "validateBBox"
    - from: "apps/napplet/tests/smoke/cases.json"
      to: "scripts/verify-phase-01.sh"
      via: "the manifest-driven runner executes the three new cases automatically"
---

<objective>
Close Gate G2 with an achievement path (bounded live audit of >=5 authoritative candidates per role until one passes fully per role), pin the exactly-two-role basemap+imagery source policy with its validator and a self-generated test fixture, put a fail-closed human checkpoint on the policy, and only then build the clean-room MapLibre/Terra Draw rectangle UI — draw/edit/clear plus the keyboard coordinate path — rendering the approved basemap through the sole adapter with request-log proof of zero direct egress. No imagery preview is implemented here; that is Plan 06.

Purpose: MAP-05/06 die if "no source found" is treated as an acceptable terminal state (review core finding). This plan makes source selection an executed, bounded, evidence-backed achievement task, makes the human gate unable to bless fixture/unavailable as completion, and ships the MAP-01/02 map interaction only on top of an approved policy.
Output: audit script + candidate evidence, two-role source-policy.json + verify-source-policy.mjs, deterministic fixture + provenance, approved-basemap map UI with selection machine and keyboard path, three new manifest smoke cases with redacted Paja evidence. Zero installs; zero lockfile mutations (all pins landed in Plan 02).
</objective>

<execution_context>
@$HOME/.hermes/gsd-core/workflows/execute-plan.md
@$HOME/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md
@.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-04-SUMMARY.md
@apps/napplet/src/shell/resource-client.ts
@apps/napplet/src/ui/copy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bounded live source-candidate audit (>=5 per role), two-role source policy + validator, and self-generated test fixture</name>
  <files>scripts/audit-source-candidates.mjs, scripts/verify-source-policy.mjs, scripts/generate-ortho-fixture.mjs, apps/napplet/src/config/source-policy.json, apps/napplet/tests/fixtures/ortho-fixture.png, apps/napplet/tests/fixtures/ortho-fixture-provenance.json, .planning/evidence/phase-01/source-candidates.json</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (Gate G2 contract field list, "Official Pages Already Captured in the Trace", OSM tile policy source, threat pattern "Unbounded tile/ortho requests")</read_first>
  <precondition>Plan 04 complete and the ladder green; network access for read-only public GET/HEAD requests is available in this execution (this is the one sanctioned live-research task of the phase). No install runs in this plan.</precondition>
  <action>
  This is the MAP-05/MAP-06 ACHIEVEMENT path, not detection. Build `scripts/audit-source-candidates.mjs` and execute it in this task.

  Candidate list: for each role — `basemap` (a general web basemap tile source) and `imagery` (orthophoto/aerial imagery) — assemble at least 5 authoritative public or licensed candidates. Start from the research-captured official pages (OSM tile infrastructure under the OSMF tile policy; NLS Finland orthophoto WMTS; Kapsi NLS mirror; USGS imagery service) and extend to additional authoritative providers whose contracts the audit verifies live from their official documentation and endpoints at execution time — every field must come from a live capture in this run, never from model memory. Previously failed candidates (NLS 401, unresolvable Kapsi redirect) may only count toward the 5 when retested now; a stale prior failure is not evidence. The audit also accepts `--descriptor <path>`: a local operator-supplied non-secret source descriptor whose auth, if any, is expressed ONLY as a shell capability/credential reference name — the audit records the reference name and never reads, echoes, or stores a credential value.

  Per candidate, capture the FULL Gate G2 contract: scheme/host/port/path template; layer/coverage ID; tile matrix/CRS and bbox order; format; auth requirement; browser/shell CORS behavior (captured response headers); attribution text (exact string per role); dataset and endpoint terms; rate/request limits; max response bytes; timeout; redirect behavior; coverage bounds; outage behavior; capture date; status. For the basemap role additionally verify the usage-policy rules the UI must obey (identification/User-Agent requirements, no bulk scraping or prefetch, bounded visible-tile requests). Bounded audit discipline: at most 10 requests per candidate (metadata/capabilities plus at most 3 sample tiles/images), explicit per-request timeout, identifying User-Agent, GET/HEAD only, no paid endpoints, no public writes, no scraping, no custom tile server. Stop testing a role's remaining candidates at the first candidate where EVERY field passes and the sample-response behavior (status, content type, size bound, CORS) is captured — that candidate becomes the role's `live-verified` entry. Write the full per-candidate results (redacted per evidence policy) to `.planning/evidence/phase-01/source-candidates.json`.

  Write `apps/napplet/src/config/source-policy.json`: EXACTLY two role entries, `basemap` and `imagery`, each carrying the winning candidate's full contract, its own `attribution` string (basemap and imagery attribution are separate strings — never concatenated in the record), and `status: "live-verified"` — or, if a role produced no fully passing candidate, that role's entry with `status: "blocked"` plus the named failing fields of its best candidate. The file is non-secret configuration; auth appears only as a capability/credential reference name.

  Write `scripts/verify-source-policy.mjs`: default mode validates the schema and honesty — exactly two roles, all contract fields present and typed, per-role attribution non-empty, statuses consistent with `.planning/evidence/phase-01/source-candidates.json` — and exits 0 for either a well-formed live or honestly blocked record while printing its explicit outcome. `--require-live` mode exits 0 only when BOTH roles are `live-verified`; otherwise it prints each role's named failed/missing fields and a machine-readable `{"outcome":"blocked"}` line and exits non-zero. Thus schema validation can preserve an honest blocked state, but no live-acceptance gate can report it as PASS. The existing manifest runner picks the default mode up automatically in place of its former honest SKIP; `--require-live` is the gate the Task 2 checkpoint and Plan 06 consume.

  After candidate testing, inspect elapsed blocker duration per role. For every role still blocked after 30 minutes, append an honest OPS-01 entry to `.planning/evidence/fallback-ledger.jsonl` naming the integration blocker, time, failed candidates/fields, and selected next fallback (`operator descriptor re-test`, `basemap-only continuation`, or `acknowledged blocked/re-scope`), then validate with `node scripts/validate-fallback-ledger.mjs`. No fake entry is required before 30 minutes; no >30-minute blocked role may reach the checkpoint without one.

  Write `scripts/generate-ortho-fixture.mjs`: deterministically renders `apps/napplet/tests/fixtures/ortho-fixture.png` from a fixed seed (procedural pattern with a visible coordinate grid and the text TEST FIXTURE baked into the pixels) — fully self-generated, no third-party imagery, so licensing is self-owned. Write `ortho-fixture-provenance.json` recording generator script hash, seed/parameters, generation date, and the binding statements that this artifact is a test fixture only and is never acceptable as MAP-05/MAP-06 acceptance evidence or live imagery. Regenerating must be byte-identical (fixed seed, no timestamps in pixels).
  </action>
  <verify>
    <automated>node scripts/audit-source-candidates.mjs --check-evidence && node scripts/verify-source-policy.mjs && node scripts/generate-ortho-fixture.mjs --verify-deterministic && node -e "const p=require('./apps/napplet/src/config/source-policy.json'); const r=Object.keys(p.roles); if(r.length!==2||!r.includes('basemap')||!r.includes('imagery')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - source-candidates.json records >=5 candidates per role, each with full contract fields, capture dates from this run, and per-request bounds respected; any operator descriptor shows a credential reference name only
    - source-policy.json has exactly the two roles with separate attribution strings; verify-source-policy.mjs default mode exits 0; --require-live either exits 0 (both live-verified) or prints named failed fields with the blocked outcome and exits non-zero
    - The fixture regenerates byte-identically and its provenance carries the TEST FIXTURE and never-acceptance statements
    - `pnpm scan:secrets` passes over the new evidence and config; no credential value appears anywhere
  </acceptance_criteria>
  <done>Both roles have either a fully live-verified candidate or an honest blocked record with named failing fields; every role blocked for more than 30 minutes has a validated OPS-01 fallback-ledger entry; the two-role policy, its validator, the audit evidence, and the deterministic test fixture exist and are secret-clean. The checkpoint can now judge real evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING source-policy checkpoint — full two-role approval or basemap-only MAP-01/02 continuation; fixture/unavailable is never MAP-05 completion</name>
  <what-built>Task 1 executed the bounded live audit of >=5 candidates per role and wrote `apps/napplet/src/config/source-policy.json` (exactly two roles: basemap, imagery), `.planning/evidence/phase-01/source-candidates.json`, and the `verify-source-policy.mjs` validator. This checkpoint is success-oriented and fail-closed. It may fully approve both live roles, or narrowly approve a live basemap for Task 3 while the imagery role stays blocked; that narrow branch does not satisfy MAP-05/MAP-06 and Plan 06 remains fenced by `--require-live`.</what-built>
  <how-to-verify>
    1. Run `node scripts/verify-source-policy.mjs --require-live`. Exit 0 means both roles are live-verified; non-zero prints each role's named failed fields and the blocked outcome.
    2. Open `apps/napplet/src/config/source-policy.json` and, for EACH role, confirm against `.planning/evidence/phase-01/source-candidates.json`: the endpoint contract matches the captured responses; the attribution string is the provider's required wording (basemap and imagery are separate strings); the dataset AND endpoint terms permit this use; rate/bounds/timeout/bytes limits are recorded and sane; auth is absent or a shell credential reference name only.
    3. Confirm the candidate audit stayed within bounds (<=10 requests per candidate, no paid calls, no public writes) and that any previously failed candidate counted only via a fresh retest.
    4. Decide the outcome. Valid outcomes are EXACTLY:
       - APPROVE — only permitted when step 1 exited 0 and step 2 checks out for both roles. Task 3 and then Plan 06 may proceed.
       - APPROVE-BASEMAP-ONLY — permitted only when the basemap role is live-verified and imagery is honestly blocked. Task 3 may proceed for MAP-01/MAP-02 with basemap attribution; Plan 06 remains blocked until Task 1 is re-run and both-role `--require-live` passes. This outcome is not MAP-05/MAP-06 completion.
       - RE-TEST — one or both roles lack a live-verified candidate and you can supply an approved source descriptor (public endpoint or licensed source with a shell credential reference): place the descriptor file locally and rerun Task 1 with `--descriptor`, then return here.
       - BLOCKED — no descriptor available and basemap is not live: append/validate an OPS-01 fallback-ledger entry when the blocker exceeded 30 minutes, record acknowledged-blocked/re-scope in STATE.md and the phase SUMMARY; execution MUST NOT proceed to Task 3 or to any live map/preview implementation. A blocked imagery role with live basemap uses APPROVE-BASEMAP-ONLY instead and still requires the >30-minute OPS-01 ledger entry.
       Approving with a fixture-only or unavailable role as if it satisfied MAP-05/MAP-06 is not a valid outcome of this checkpoint under any phrasing.
  </how-to-verify>
  <resume-signal>Type "approved" (both roles live-verified), "approved-basemap-only" (basemap live; imagery explicitly blocked and ledgered if >30 minutes), "re-test: {descriptor path}", or "blocked: {reason}"</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Clean-room MapLibre/Terra Draw bbox UI — draw/edit/clear + keyboard coordinate path on the approved basemap through the sole adapter, with manifest smoke cases and no-egress proof</name>
  <files>apps/napplet/src/map/map-view.ts, apps/napplet/src/map/source.ts, apps/napplet/src/ui/selection.ts, apps/napplet/src/ui/app.ts, apps/napplet/tests/unit/source-policy.test.ts, apps/napplet/tests/unit/selection.test.ts, apps/napplet/tests/smoke/paja-harness.mjs, apps/napplet/tests/smoke/cases.json</files>
  <read_first>.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-UI-SPEC.md (Interaction State Machine, Accessibility Contract, Layout, Copywriting Contract, Component Inventory rows 2-6 and 10-12), .planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-RESEARCH.md (Gates G1/G4, named cases 19-20), apps/napplet/src/bbox/area.ts, apps/napplet/src/ui/copy.ts, apps/napplet/src/config/source-policy.json</read_first>
  <precondition>Task 2 resumed with "approved" and `--require-live` exits 0, OR with "approved-basemap-only" and the basemap role alone is live-verified while imagery is explicitly blocked (and ledgered if >30 minutes). If the checkpoint outcome was blocked/re-test, this task does not run. Plan 06 always retains the both-role `--require-live` precondition.</precondition>
  <behavior>
    RED before GREEN for the pure modules:
    - Test 19 `source_policy_rejects_unapproved_origin_layer_or_scheme`: source.ts request construction throws/rejects for a host not in the policy, an http downgrade of an https template, an unapproved layer ID, and a non-template path — for BOTH roles; approved basemap tile coordinates produce exactly the policy's path template expansion.
    - Test 20 `attribution_is_present_for_active_source`: attribution composition returns the basemap role's exact policy string when only the basemap renders, and the two-source variant only when imagery is additionally live.
    - `attribution_basemap_only_has_no_dangling_separator`: the basemap-only variant contains neither the separator glyph nor any imagery wording — basemap and imagery attribution are separate strings joined only when both render.
    - selection.test.ts covers every UI-SPEC selection-machine transition as pure reducer tests: draw arm/complete/Escape, exactly zero-or-one rectangle (a completed second draw replaces, never silently mid-draw), edit release re-validate to SELECTED_VALID or SELECTED_INVALID with the first-failing clause's code, Apply coordinates atomic validation (field errors leave state unchanged), Clear to CLEARED_UNDOABLE with the prior bbox retained, Restore within 10s returning the exact prior state, toast expiry to EMPTY.
  </behavior>
  <action>
  RED first: write `tests/unit/source-policy.test.ts` and `tests/unit/selection.test.ts` against the not-yet-existing modules; confirm failure; commit. Then GREEN:

  `src/map/source.ts`: loads `config/source-policy.json`; exports the per-role allowlist check (origin, scheme, layer, template shape, format), `basemapTileUrl(z,x,y)` expanding only the policy template, and `composeAttribution(active: { basemap: boolean; imagery: boolean })` returning per-role strings from the policy — basemap-only renders one string with no trailing separator; both-live joins with the single separator glyph. No network code here.

  `src/ui/selection.ts`: the pure selection machine from the UI-SPEC Interaction State Machine, validating every release/apply through `validateBBox` (Plan 04) and mapping codes to copy via `errorCopyFor`. No DOM, no map import — the reducer is the tested contract.

  `src/map/map-view.ts`: clean-room MapLibre (approved Plan 02 pin; no 21maps code/assets/config per the recorded MAP-08 provenance decision — verify:map-provenance must stay green) with an INLINE style object: one raster source whose tiles use a registered custom protocol (`maplibregl.addProtocol`) so that EVERY tile request flows through source.ts allowlisting into `loadApprovedBytes` on the sole shell adapter — MapLibre gets no URL it can fetch directly, and no remote style/glyph/sprite/font reference exists (raster-only needs none). Terra Draw (approved pin + MapLibre adapter) supplies rectangle draw/edit with 4 corner handles and interior move; wire its events into the selection machine. Handle devicePixelRatio rendering, resize, and clean map/draw teardown (Gate G4 items), and set the map's attribution control from `composeAttribution` (basemap-only at this point in the phase).

  `src/ui/app.ts`: replace the placeholder map region with the real map + toolbar (Draw bounding box accent CTA, Enter coordinates, Clear selection with error treatment; Stop drawing while armed), the coordinate field group (four labeled decimal-degree inputs, `inputmode="decimal"`, monospace values, Apply coordinates) as the guaranteed keyboard path, area readout, undo toast (`role="status"`, 10s, pauses on hover/focus, persistent under reduced motion), status banner (`role="alert"` for validation errors), and the `aria-live="polite"` announcement region — all copy from the frozen COPY module, all styling from tokens.css only, 44px targets, focus-visible ring, skip-link/tab order per the Accessibility Contract. Drawing is armed explicitly — touch drag never hijacks pan. The request-preview DTO panel updates from `buildRequestPreview` on every SELECTED_VALID. The preview pane itself keeps the truthful "Source unavailable"-style non-implementation state: NO imagery preview logic ships in this plan.

  Smoke: extend `paja-harness.mjs` with three cases and APPEND their manifest entries to `cases.json` in this same task (the runner executes them automatically): `draw` (MAP-01 — arm, drag, release; assert exactly one rectangle, area readout, DTO bbox at 6 decimals; assert the rendered attribution equals the policy's basemap string with no imagery wording) · `edit-clear` (MAP-02 — corner-handle resize and interior move re-validate on release; Clear shows the toast, Restore selection restores the exact bbox; then clear-to-expiry empties) · `coords-keyboard` (keyboard-only bbox create + edit via the coordinate fields and Apply coordinates, focus ring visible in the capture). EVERY case asserts the request-log contract: the harness allowlist is loopback origins plus nothing else at the page-request level — any direct MapLibre worker/image/CSS/fetch egress to a non-loopback host fails the case — while basemap bytes are evidenced through the adapter path (custom-protocol requests resolved by the shell resource capability) with only visible tiles requested, satisfying the policy's bounded-request rules. Write per-case redacted JSON + screenshots under `.planning/evidence/phase-01/`.
  </action>
  <verify>
    <automated>pnpm --filter @terrcvm/napplet test:unit -- source-policy selection && pnpm --filter @terrcvm/napplet typecheck && pnpm --filter @terrcvm/napplet build && node scripts/verify-dist.mjs && node scripts/verify-shell-boundary.mjs && bash scripts/verify-phase-01.sh</automated>
  </verify>
  <acceptance_criteria>
    - RED commits precede GREEN; named cases 19-20, the no-dangling-separator test, and the full selection-machine suite pass
    - The built artifact under Paja passes draw, edit-clear, and coords-keyboard VIA THE MANIFEST RUNNER, each with a loopback-only page-request log and adapter-evidenced basemap tiles
    - map-view.ts contains an addProtocol registration and an inline style; verify-shell-boundary and verify:map-provenance stay green (sole adapter untouched outside src/shell/, clean-room upheld)
    - The attribution overlay in the draw-case screenshot shows the basemap policy string only, with no separator artifact; no imagery preview code path exists in this plan's diff
  </acceptance_criteria>
  <done>MAP-01/MAP-02 are proven on the built artifact over the approved basemap with the exact UI contract and keyboard path; MAP-06's basemap attribution and bounded-request behavior are evidenced in the request log; the manifest carries the three new cases for every future ladder run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit script → public endpoints | Live read-only probing of candidate sources crosses to the open internet |
| Operator descriptor → audit/evidence | A locally supplied descriptor may reference credentials that must never be recorded |
| MapLibre/Terra Draw → network | Map libraries default to direct fetch; all bytes must be forced through the sole adapter |
| Human checkpoint → phase progress | A soft approval would launder fixture/unavailable into MAP-05/06 completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-25 | Spoofing | Source policy claiming live without proof | high | mitigate | verify-source-policy honesty check against candidate evidence; --require-live gate; checkpoint valid outcomes exclude approving non-live roles |
| T-01-26 | Information Disclosure | Credential value in descriptor/evidence/config | high | mitigate | Credential expressed only as shell reference name; audit never echoes values; scan:secrets over evidence and policy; public-diff review |
| T-01-27 | Denial of Service / policy abuse | Unbounded candidate probing or tile scraping | medium | mitigate | <=10 requests per candidate, GET/HEAD only, identifying UA, per-request timeout, visible-tiles-only at runtime per basemap policy |
| T-01-28 | Tampering | Direct MapLibre worker/image/CSS/fetch egress bypassing the adapter | high | mitigate | Custom protocol as the only tile path, inline style with no remote refs, loopback-only page-request assertion in every smoke case, static shell-boundary scan |
| T-01-29 | Repudiation | 21maps material copied without provenance | medium | mitigate | Clean-room implementation per the recorded MAP-08 decision; verify:map-provenance remains a hard ladder step |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero installs in this plan; lockfile-unchanged criterion via verify-lock-approved in the ladder; public-diff review at wave end |
</threat_model>

<verification>
- [ ] `node scripts/verify-source-policy.mjs` (schema) green after Task 1; `--require-live` outcome recorded honestly (0 or named-fields blocked)
- [ ] Checkpoint outcome recorded as approved / approved-basemap-only / re-test / blocked; basemap-only unlocks only Task 3 and never counts as MAP-05/MAP-06 completion
- [ ] `bash scripts/verify-phase-01.sh` green end-to-end after Task 3, including the three new manifest cases with loopback-only request logs
- [ ] Wave-end secret/public-diff gate: `pnpm scan:secrets` exits 0 over repo + dist + new evidence; `git diff --check` clean; no lockfile delta in this plan's commits
</verification>

<success_criteria>
- Source achievement executed: >=5 bounded live candidate tests per role with full-contract evidence, stopping at the first fully passing candidate per role; two-role policy pinned with separate attribution strings
- The human gate is fail-closed and was exercised: full approval only with both roles live-verified; narrow approved-basemap-only may deliver MAP-01/02 while Plan 06 stays fenced; otherwise re-test or acknowledged-blocked — fixture/unavailable never approved as MAP-05/06 completion
- MAP-01/02/06/08 proven on the built artifact: rectangle draw/edit/clear + keyboard path with the exact UI contract, basemap-only attribution with no dangling separator, all bytes through the sole adapter, zero direct egress, clean-room provenance intact — and no imagery preview implemented
</success_criteria>

<output>
Create `.planning/phases/01-sandboxed-bbox-and-orthophoto-ui/01-05-SUMMARY.md` when done
</output>
